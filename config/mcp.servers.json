{
  "mcpServers": {
    "windows-mcp": {
      "transport": "stdio",
      "command": "uvx",
      "args": [
        "windows-mcp"
      ],
      "framing_mode": "newline-json",
      "env": {}
    }
  }
}

<!-- Moved out of AGENTS.md to keep the always-loaded context lean. Paths/links are relative to the repo root. -->

## Debugging via logs

Aiko writes a single, level-disciplined log stream that lands in three places at once: stderr (live console), `data/app.log` (rotating, 5 MB × 5 files), and an in-process ring buffer (last 1000 lines) exposed over MCP. Every line is shaped the same way — memorise the shape and the canonical fields and you can answer most "what just happened?" questions with one or two MCP calls.

### a. Where to look (priority order)

1. **`tail_logs(n=200, level="DEBUG", module_contains=…)`** — first stop. Instant, scoped, level-filterable. Use `module_contains="prompt"` to focus on `app.core.session.prompt_assembler`, `module_contains="ollama"` for the LLM client, etc.
2. **`read_log_file(lines=2000, grep="turn=abc12345")`** — when the issue is older than the ring (~1000 lines back) or you want to follow one turn end-to-end. Hits the rotating `data/app.log` (and its `.1`…`.5` siblings).
3. **`data/app.log` directly via Read tool** — for offline / cross-session forensic work, including reading rolled siblings.
4. **`terminals/*.txt`** — last resort for noise outside the `app.*` loggers (uvicorn, fastmcp, library warnings).

`get_log_config` returns the current effective configuration (level, file path, module overrides, ring usage).

**Frontend crashes are not in this stream.** They arrive over HTTP from the browser and are recorded separately — start at `get_ui_crashes()`, see §g.

**Nor are native crashes.** If the process *vanished* rather than logged an error, nothing in this stream will explain it — start at `get_native_crashes()`, see §h, and do not trust the Python frame in the `faulthandler` dump.

**High-frequency streams are recorded as runs, not lines.** The browser bridge (`web/src/log.ts`) folds its noisy sources — lipsync amplitude at 30 Hz, one frame per streamed token, mic level — into a single `ui[ws] audio_amplitude {"burst": 312, "seconds": 10.4}` per run, emitted when the stream goes quiet or every 30 s while it continues. Before that, one real session was 2342 identical amplitude lines, **a third of the whole file**, in front of everything worth reading. The same principle applies to anything you add here: if the count is the information and the individual line is not, use `debugLog.logBurst` and let the count carry it. On the backend side the idle scheduler's per-worker `run start` and its uneventful `idle_workers tick` lines are DEBUG for the same reason — `run done` (the one with the result payload) stays INFO, and a tick that actually turned work away still reports at INFO because that is the state §c sends you here to diagnose.

**One stream, one exception.** The P44 prompt-cache telemetry writes JSONL to `data/prompt-cache.jsonl` instead, through an `app.promptcache` logger with `propagate = False` — a per-turn record would bloat `app.log`, and its only consumer is a script. It is **off by default** (`logging.prompt_cache_log_enabled`); turn it on for a measuring session and read it back with `python scripts/prefix_break_report.py`. See [`docs/prompt-caching.md`](../docs/prompt-caching.md#measuring-where-the-prefix-breaks-p44).

### b. Standard line shape

Every record is formatted as:

```
[YYYY-MM-DD HH:MM:SS,mmm] LEVEL [logger.name turn=abc12345] message text key1=val1 key2=val2
```

`turn=…` is an 8-char hex correlation id allocated by `TurnRunner.run()` and propagated via a `contextvars.ContextVar` ([`app/core/infra/log_context.py`](../app/core/infra/log_context.py)). When no turn is active you'll see `turn=-`. The canonical structured fields (memorise these for precise greps):

- **Turn lifecycle**: `model=`, `ctx_pct=`, `prompt=`, `completion=`, `first_token_ms=`, `total_ms=`, `eval_ms=`, `tools=`, `compactions=`, `mood=`, `aborted=`, `chars=`, `filler=`.
- **Prompt assembly**: `ctx=`, `budget=`, `est_tokens=`, `sys=`, `hist=`, `user=`, `rag_tokens=`, `history_msgs_in=`, `history_msgs_out=`, `inner_blocks=`, `summary_active=`, `compaction=`, `aggressive=`.
- **Scheduler**: `jobs_run=`, `elapsed_ms=`, `queue_after=`, `idle=`, `names=`, `submitted=`, `ran=`, `cancelled=`, `windows_opened=`.
- **Ollama**: `msgs=`, `tools=`, `stream=`, `prompt_tokens=`, `completion_tokens=`, `tool_calls=`, `format_json=`, `stopped=`.
- **STT**: `chars=`, `duration_ms=`, `model=`, `language=`, `init_ms=`, `device=`, `sample_rate=`, `max_s=`, `silence_s=`.
- **TTS**: `voice=`, `provider=`, `init_ms=`, `chunk_chars=`, `speed=`, `generate_ms=`, `played_ms=`, `queue_depth=`, `drained_chunks=`.
- **Proactive**: `source=` (`prepared`|`llm`).

### c. Symptom → grep target

| Symptom | First check |
|---|---|
| **Aiko silent / no reply** | `tail_logs(level="DEBUG", module_contains="turn_runner")` → find the failed `turn=…`, look at `first_token_ms=` and surrounding ERROR lines. Then `read_log_file(grep="turn=<id>")` to grab everything correlated. |
| **The whole process died / "Aiko crashed" with nothing in the log** | `get_native_crashes()` — a native memory fault, not a Python error. Read `module` (the DLL owning the faulting address), `thread_name` (whether it faulted on a thread of ours at all) and `duplicate_openmp`, **not** the Python frame in the `faulthandler` dump, which is an innocent bystander. The `dump` block carries the minidump already parsed. §h. Rule out a hardware theory early: an access violation is a pointer bug, and memtest/OCCT passing tells you nothing about it. |
| **RAG search feels slow / `data/lancedb` is huge / a native fault in `_lancedb.pyd`** | Check the *physical* shape, which no log line prints: `RagStore.fragmentation()` reports `files` / `bytes` / `versions` / `rows` per table. Lance writes a new fragment per upsert and retains the old one, so **the number to look at is files per row** — 26,765 files and 1.09 GB for 6,175 rows, ten retained versions per row, is what "nobody ever compacted it" looks like, and it compacted to 10 files / 27 MB (H33). `RagStore.optimize()` is the fix and [`rag_maintenance_worker.py`](../app/core/rag/rag_maintenance_worker.py) keeps it fixed; if that worker is not running, `SELECT value FROM kv_meta WHERE key LIKE 'rag_maintenance%'` is its watermark. Nothing here is data-loss risk — the mirror re-derives from SQLite on boot — but **do not open the live store from a scratch script with hand-typed `embedding_model` / `vector_dim`**: a mismatch is read as an embedding swap and drops every table. Use `auto_open`. |
| **Voice mode never picks up** | `set_log_level("app.stt.realtime_stt_service", "DEBUG")`, then `tail_logs(module_contains="stt")`. Look for missing `STT engine ready:` INFO or repeated capture errors. |
| **TTS stutters / drops** | `tail_logs(module_contains="tts")`. Cross-reference `TTS enqueue:` / `TTS play done:` DEBUG with `tts state:` transitions in `app.tts_queue`. |
| **A TTS engine was selected and she sounds exactly the same** | This was the shipped behaviour until Aug 2026: `tts.provider` was stored, exposed over the API and switchable at runtime, while `_build_tts_service` hardcoded `PocketTtsService` — so every value meant pocket-tts and no test noticed, because "does TTS work" passed throughout. It now goes through [`app/tts/registry.py`](../app/tts/registry.py). **Read `GET /api/settings` → `tts.providers` first**: each row carries `available` plus a `reason` naming the install command, and an engine whose venv is missing is *refused* rather than silently substituted (grep `refusing TTS provider`). If it was accepted but degraded anyway, `registry.build_with_fallback` logs `TTS falling back from X to Y` at WARNING. **Never conclude from the setting alone that an engine is in use** — ask the engine: `ChatterboxTtsService.describe()` reports the sample rate, cloned voice and the device it *actually* got. |
| **A Chatterbox engine stutters, or CUDA "did nothing"** | Two separate traps. (1) **Device**: the app cannot verify CUDA without importing torch, which it refuses to do, so `tts.providers.<name>.device` is only a request — the sidecar's `_device_report` checks on load and fails with a message naming the cause. Both engine venvs carry **CPU-only torch wheels**, and Chatterbox pins `torch==2.6.0`, which cannot drive a Blackwell card (a 5090 is sm_120, needing torch 2.7+ / cu128); the route through is a Python 3.14 venv, where upstream itself allows `torch>=2.9.0`. (2) **Threads**: torch defaults to one thread per core and that is *slower* here — Nano measured RTF 0.78 on 8 threads against 0.93 on 16 — so a hand-set `threads: 16` is a pessimisation. Leave it at `0` for `registry.default_threads()`. Also note Chatterbox generates the whole clip before emitting a byte, so first-audio *is* the full synthesis and grows with sentence length (~3.5 s for a 4.3 s sentence); only the first sentence of a reply pays it, thanks to `TtsQueue`'s prefetch. |
| **Long unnatural pauses *between* sentences (worse on Chatterbox than pocket-tts)** | The prefetch is not landing, so each sentence is synthesised cold while the listener waits — 2.5 s a boundary on Nano. Do **not** read the pause as the cadence layer's: `pause_before_ms` / `pause_after_ms` are capped at 1500 ms and overlap generation, so anything longer is waiting, not intent. Confirm with `set_log_level("app.tts", "DEBUG")` and compare `generate_ms=` against `played_ms=` on consecutive `TTS play done:` lines — a prefetched sentence shows `generate_ms` near zero, since playback found the clip in the cache. If every sentence shows a full `generate_ms`, the prefetch is missing, and there are only a few ways that happens: the engine's `generate_audio` doesn't populate a `ClipCache` (so the lookahead's work is discarded and playback regenerates), the cache key includes something playback resolves differently (speed did, historically), or `_spawn_lookahead` is not reaching the sentence — it scans past silence/earcon chunks and dedups on text, so a stale `_lookahead_text` suppresses it. The mirror-image symptom is a **long silence at the top of the reply**: that is the prefetch winning the engine ahead of the spoken sentence, which `SynthesisGate` plus the spawn ordering in `_dispatch` exist to prevent. See the prefetch section in [`docs/tts-engine-options.md`](../docs/tts-engine-options.md). |
| **"Her warmth / level keeps changing between sentences"** | Measure before you tune, because the two halves are different things and one of them is already fixed. **Level**: after [`app/audio/loudness.py`](../app/audio/loudness.py) the gated spread is 0.00 dB and 0.23 dB K-weighted, so a level complaint is almost always brightness being *heard* as level — a brighter sentence reads as louder and closer. **Warmth**: synthesise the *same* sentence five or six times and compare the low/high band ratio (`app.audio.timbre.spectral_tilt_db`). Identical text drifting ~4 dB is the model re-sampling timbre per call, which is what [`app/audio/timbre.py`](../app/audio/timbre.py) shelves out (5.79 → 0.69 dB across a turn); a target of `None` in the log line `matching brightness to …` means the reference clip could not be measured and the stage is off. Three things this is *not*, all measured: not sampling parameters (`temperature`, `cfg_weight`, `min_p`, `repetition_penalty` all land in the same band — and a single-run win here will not reproduce, so always repeat), not the engine being worse than pocket-tts (pocket drifts *more* on centroid, 394 Hz against 245 Hz), and not new — fixing the inter-sentence pauses is what made it audible. |
| **"One sentence was great and the next was much slower"** | Check whether anything *asked* for a different speed before you touch the pacing code, because usually nothing did. `agent.tts_runtime_speed_enabled` is off by default, and with it off `resolve_playback_speed` pins every sentence to the same multiplier — so on a default install a within-reply tempo change cannot be coming from affect, the cadence layer, or the slider. It is the engine: synthesising the *same* sentence six times moves the delivered tempo 10.7–20.9%, and adjacent draws land up to 23% apart. That is what [`app/audio/speech_rate.py`](../app/audio/speech_rate.py) stretches out (24.1% → 5.9% of spread, 8 of 8 sentences within 5% of her pace); `tts.speech_rate_match_limit` is the ceiling and the log line `matching tempo to …` carries the adopted target, whose absence means no `manifest.json` was found beside the reference clip and the stage is off. Two traps if you measure this yourself: **count syllables, not characters** — a characters-per-second reading made the long sentence look 14% slower when in syllables per second the two were identical, purely a letter-density artefact — and measure over *voiced* seconds, or a comma reads as slow speech. Note this is the third per-clip drift in the same family, after level and brightness; if you are here for a fourth, the general lesson is that a generative engine re-samples everything about her voice on every call. |
| **Her whole voice is slow (or fast) after changing the reference clip** | Not the pacing code and not a sample-rate mishandling — the sidecar hands the reference to Chatterbox, which resamples it itself, and the wav header is the engine's own rate. **Chatterbox clones speaking rate along with timbre**, so the reference's own delivery is the cause. Measured on identical text: a reference of 10 clips at a 0.92 s median delivered 5.26 syl/s against 6.83 from her 12-part, 3.04 s-median incumbent — 24% slow, i.e. past the `tts.speech_rate_match_limit` cap and unfixable downstream. The brightest clips in a found voice pack are single drawled words, which is exactly how this gets built by accident. Diagnose with the studio's **Check pace** (`POST /api/pace`), which speaks one fixed probe through any candidate *or saved* voice and reports delivered syl/s against her 6.55; `tools/tts_lab/refset.py:shape()` names the cause (`median part 0.92s — isolated words`). Fix by rebuilding from clips over `MIN_CONNECTED_S` (1.4 s). If a residual few percent remains and the clips are not English — so no transcript can supply a measured target — tick **hold her to her own pace**, which writes `target_syl_s` into the manifest; `_adopt_rate_target` honours a declaration ahead of measuring parts. |
| **One word is reliably mangled, or she sounds robotic on a particular phrase** | First settle whether it is reproducible: synthesise the same text two or three times. A fault that *moves* is a sampling draw; one that stays on the same word is the text or the reference. Utterance-initial /h/ is the worst case measured ("Hey, …" — aspiration with no acoustic context before it), and it clears at a colder temperature. Every such knob is **paid for in tempo**, so measure the cost: on Nano, `min_p=0.05` (shipped *off*, and upstream's recommended stability knob) costs about as much as `temperature=0.5`, ~8 points of pace each. Do **not** escalate to a bigger variant — `chatterbox-full` delivers 3.91 syl/s against Nano's 5.66 on identical text at RTF 2.72, so it trades an artifact for a slower, non-real-time voice. Knob names and defaults are **per engine and not portable**: Nano ships `min_p=0.0`, `exaggeration=0.0`, `cfg_weight=0.0` where the full model ships `0.05 / 0.5 / 0.5`, so published "lower cfg_weight to 0.3" advice is about a different model. Read the installed signature (studio's **Read this engine's knobs**, or the sidecar's `defaults` in the `load` reply) rather than a model card. To make a value stick, save the voice from the studio — its `manifest.json` gets a `generate` block **keyed by engine**, so one reference can hold different numbers for Nano and Turbo and each reads only its own — or set `tts.providers.<name>.generate`, which outranks it. The log line `generating with …` names what was adopted; `carries tuning for … but not for this engine` means another engine's entry was correctly declined; silence on both means shipped defaults. |
| **"She is faster on one engine than the other"** | A knob the engine never had, failing silently. `_apply_assistant_preferences` wires the pacing slider and the affect-speed gate through `getattr`, so an engine missing `set_length_scale` / `set_runtime_speed_enabled` skips both without a word: `assistant.tts_length_scale` does nothing, and `agent.tts_runtime_speed_enabled=false` fails to suppress the cadence layer's per-sentence hints, which then apply *uncapped* (an excited sentence at 1.12× where the other engine pinned 1.00×). Chatterbox shipped this way. Check the engine exposes both — `tests/test_tts_pacing_parity.py` is the guard — and take the final rate from `reactions.resolve_playback_speed()` rather than reimplementing the gate. |
| **A fragment of speech plays *after* the sentence finished** | H46, and it is neither the text nor the engine — check those off cheaply rather than reasoning about them, because both are plausible and both are innocent. The **text** path is fuzzed by `_probe_leak`-style streaming (14 tag shapes at 1/3/7 chars per delta through `safe_visible_prefix` → `drain_tts_stream_chunks` → `prepare_tts_text`, zero leaks including unclosed tags and a `]` inside one), and the **clip** is measurable: segment into speech/silence runs and ask whether the last run is detached by a real gap — 0 of 8 clips were, tails being 0.22–0.40 s at 2–6% of body RMS, i.e. decay. Do not measure the tail against an energy gate; anything loud enough to hear is classified as speech and the measurement hides its own subject. What is left is the **contract between the pre-roll and the client**: `PcmPlaybackMixin._PRE_ROLL_CHUNKS = 5` ships ~250 ms ahead of real time, so the browser permanently holds speech that has not been heard, and `audio_end` deliberately does *not* discard it (the next sentence chains onto the tail). A cut therefore left the pre-roll playing with nothing able to retract it. The retraction is now `0x14 audio_cancel`, sent only for a cut: grep `audioCancelled` in the client debug log, and note that a *natural* end must never cancel or every sentence loses its tail — `tests/test_pcm_cut_cancels.py` and the `AudioOutputManager cancel` suite assert that pairing from both sides, which is the only place it is expressible. |
| **Wrong context retrieved** | `set_log_level("app.core.session.prompt_assembler", "DEBUG")`, replay the turn, then read the `prompt built:` line for `rag_tokens` / `providers` / `slowest_provider` / `history_msgs_in/out`. |
| **Memory not recorded** | `tail_logs(module_contains="memory")`, look for `memory:` INFO lines from `turn_runner`, confirm the extractor ran. |
| **A worker that "runs fine" has never actually produced anything** | Absence-shaped bugs do not appear in logs — a silent early return logs nothing, and the fact-checker sat at one web search in three months because `_maybe_enqueue_claims` got a dict where it expected a `Memory`. **Count rows, don't read logs.** For F1: `SELECT value FROM kv_meta WHERE key='fact_checker.queue'` (persistently `[]` with claim-bearing memories present is the tell), `fact_checker.rate_state` (`last_allow_at` months old = it has not searched), and `SELECT COUNT(1) FROM memories WHERE json_extract(metadata,'$.last_verified_at') IS NOT NULL` (zero = no verdict was ever applied). Then run [`scripts/fact_check_backfill.py`](../scripts/fact_check_backfill.py) to replay the stored corpus through the real gates and see what *should* have queued; its skip histogram names the gate responsible. The same shape applies to any hook-fed worker — `user_correction_worker` logging `no_candidates` on 1079 consecutive runs is the same class of signal. |
| **A worker runs on schedule and skips every single time** | The complement of the row above: here the worker *does* log, and the line reads like an idle beat. `rg -o "<name>-worker skip: [a-z_ ]+" data/app.log \| sort \| uniq -c` — a **100% skip rate with a single reason** is never a coincidence, and it separates "nothing to do" from "cannot do anything" in one command. The belief and promise workers sat at 44/44 and 88/88 `no recent turns` for months because they were wired with the bare `session_id` while `messages.session_id` holds the scoped `f"{user_id}:{session_id}"` (H19). **Any chat-db read must key on `session_key`, never `_session_id`.** Note the idle scheduler will not rescue you here: zero pressure from a broken demand probe still gets **heartbeat**-admitted, so a worker with an unreachable input runs forever and accomplishes nothing. Both workers now log at WARNING when their key matches no message at all. |
| **An LLM worker runs, burns 30+ s of GPU, and reports zero items** | Suspect the *answer budget* before the prompt. `rg -o "answer truncated: surface=\w+" data/app.log \| sort \| uniq -c` and `rg "thinking starved" data/app.log`: on a `think=True` call the hidden trace shares `num_predict` with the answer, and a trace of 1.6k–5.1k tokens against a 400-token answer budget returns `done_reason="length"` with **empty content**. The tell is that `completion_tokens` is identical to the token on every line for a given surface (`num_predict` + `think_num_predict_headroom`) — that is a budget wall, not model variance. 98 such calls across 10 surfaces, and the promise + belief workers persisted **nothing** for eight and ten weeks (H27). Two follow-ons. **(1) Read the failure reason, not the count.** `promises=0` must mean "the model answered, and the answer was empty"; if a worker collapses "no answer" into `[]`, every run reports success — the reasons are now distinct (`llm_empty_answer` vs `llm_unparseable` vs `llm_error`), and only the middle one is about parsing. **(2) `llm-unparseable` is only actionable with the payload.** Both workers logged the verdict and not the answer for months, which hid a plain schema mismatch: `format_json=True` returns an *object*, and both prompts asked for a bare array. Use [`app/llm/json_answers.py`](../app/llm/json_answers.py) (`parse_json_array_answer`) rather than a fresh `json.loads` — it accepts the object wrapper, a bare array, `{}` as "nothing found", a drifted key, and an array embedded in prose. Fixing a per-surface `think` flag is the wrong layer: the same symptom was patched at two call sites in June and was still live at ten in August. |
| **Fact-checker never verifies anything / verdicts look arbitrary** | Two different failures, and the log tells them apart. **No supply**: `rg -c "fact-check enqueued" data/app.log` returns 0 while `rg -o "enqueue skip: [a-z]+" data/app.log \| sort \| uniq -c` shows a single dominant reason — for `personal` that is usually *correct* (the memory-level name check is the only gate that knows a row is *about a person*; the token scrubber only removes the name, not the sensitive content, so do not "unblock" it). **Bad payload**: run `python scripts/fact_check_backfill.py --limit 5000 --json` and read the `sentence` beside each `text`. `text` is the search query and must stay narrow; `sentence` is the proposition the model adjudicates and must actually assert something. A claim reaching the model as a bare span (`"2026"`, `"The Rent"`) means the sentence was lost — F1 verified spans for months and 0 of 90 contained a verb (H20). A spurious `contradict` there is not harmless: `|delta| > 0.2` replaces `memory.content` outright. **The outbound query is a third failure with the same root**: a span scrubbed of names can be safe and still unusable (`'playful moments with Jacob'` → `'playful moments with'`). F6's reformulator now receives the sentence as `CONTEXT` alongside the span, so check `rg "query reformulated" data/app.log` — output byte-identical to the input on most lines means it is being called with nothing to work from (27 of 34 before this), and a query that gained a *guess* rather than a predicate (`Back Camp` → `Back Camp TV series`) is the signature of a span with no verb. Planner-authored queries pass `already_neutral=True` and skip the rewrite by design; the deterministic scrub still runs on them, and that scrub is the only leak guard, so never gate a search on anything else. |
| **iOS PWA: no TTS after locking/unlocking the screen, but the phone shows audio playing** | The context is a zombie — iOS reclaimed the audio unit while the app was backgrounded and `resume()` still resolves to `state === "running"`. Every guard reads healthy, so nothing is dropped and nothing is logged; the media indicator is the keep-alive loop, not speech. `AudioOutputManager.onForeground()` now **replaces the context after any background stint** rather than inspecting it (`[ui] audio contextReplaced` with `reason=foreground`); see [`docs/voice-mode.md`](../docs/voice-mode.md). If you see `audio dropSuspended`, that is the honest-suspend failure — it needs a user gesture, and the "Sound is off" readout plus one tap is the intended path. **Two traps that made this recur.** (1) The original fix probed `currentTime` and rebuilt only a *frozen* clock. iOS also hands back contexts whose clock advances while the output reaches nothing, and no page-readable signal separates that from health — the analyser sits before `destination` and reports the samples it feeds into a void. **Do not re-add a health check as the gate**; a live clock proves rendering, not audibility. (2) `logging.ui_log_categories` must contain `"audio"` or the endpoint drops every one of these lines *silently*; it was missing from `config/default.json`, so this whole row was ungreppable until 2026-08. Confirm the plumbing works before concluding "no logs" means "nothing happened". Settings → "Restart sound" calls `restart()`, which is the same unconditional replacement — it used to call `onForeground()`, a no-op on a context already reporting `running`, which is why force-quitting was the only cure. |
| **Mobile: she answers in text but the voice never arrives ("it disappeared again")** | Before reading anything into the audio stack, **check whether the phone was even connected.** Binary TTS goes only to the elected audio owner and is **never replayed**, so a socket that is down for a turn loses that turn's speech permanently — the transcript still arrives on reconnect, which is exactly what makes it read as "her voice disappeared" rather than "my phone dropped". `rg "audio-owner elected: owner=None clients=0" data/app.log` marks the start of every deaf window. Rule the audio path out in one look: healthy `[ui] audio clipStart` lines carrying `"state": "running"` with a ~0.1 s `lead`, no `dropSuspended`, and **no `contextReplaced`** mean playback was fine and the connection is your problem — that combination is what separates this from the zombie-context row above, which otherwise presents identically. Then read the drop itself from both ends: `rg "ws client (connected\|gone)" data/app.log` gives `up=` (socket lifetime), `silent=` (quiet before the close) and `remaining=`, and `rg "\[ui\] ws close" data/app.log` gives the peer's view. **Three causes, and `visibility` + `silent` name which:** `visibility=hidden` → the OS suspended the page; `visible` with `silent≈ws_ping_timeout` (20 s, set in [`app/web/runner.py`](../app/web/runner.py)) → the link died quietly and uvicorn's ping timer hung up; `visible` with a small `silent` → the connection was reset outright. Finally read `downSeconds` on the following `[ui] ws open`: the client reconnect is a **flat 1.5 s with no backoff**, so a gap of minutes means the retry timer never fired — the page was frozen, not the backend unreachable. The `[ui]` half needs Debug logging on; the two server lines are always written. |
| **A pooled cue is armed constantly and never surfaces** | **Run `python scripts/cue_reach_report.py` — do not hand-write the query.** Roughly half of all decline rows are *ineligible* (`cadence_block`, `no_opening`, `no_stock`, `question_balance`): the cue never had a chance that turn, so counting them makes healthy cues look dead. Four cues were written off that way — `self_callback` "2 of 452 on `cadence_block`" is 2 of **27** real chances, and `caught_mid_activity` "2 of 265 on `no_stock`" is 2 of **19** — after H30 had already built the eligible denominator to prevent exactly that (H30 retraction). The script imports `is_eligible_decline` from production rather than restating the list, and **aggregates eligible declines by gate across all cues**, which is the view that matters: five cues at 2–16% reach were one gate, `topic_miss`, at 96.8% of every eligible decline. **That headline was itself wrong twice over, and H43 is the row below — read it before acting on a `topic_miss` figure.** In short: much of the bucket was `cadence_block` mislabelled, and the gate it names accepts 33.2% of all candidates, so it cannot be what declined those turns. **An empty denominator is a third case, not a 0% row** — undefined reach, printed separately, and it fooled a reader once more even after the retraction above ("is a gate that closed 399 times a rate limit or a deadlock?"). Do not answer that from the counts: the script resolves each cue's cooldown against its own `CUE_POLICIES` entry and prints the verdict, e.g. `self_callback` → `inside cooldown, by design (cooldown 240h, last surfaced …, +78.5h remaining)`, its two surfacings sitting exactly 10.0 days apart. **The only cadence shape that is a bug is a `cadence_block` dated after `last_surfaced_at + cooldown`**, which gets its own section. Only then read reasons per cue. `topic_miss`, `importance_floor`, `cadence_block`, `no_stock` and `cross_lane` come from the provider's own bail point ([`cue_accounting.py`](../app/core/proactive/cue_accounting.py)); `lost_priority:<cue>` and `question_balance` are attributed structurally. **`provider` is the catch-all and means the provider declined without saying why** — a table still dominated by it points at an uninstrumented bail rather than at a cause. **Check the window first**: bare `provider` stops at zero on 13 Aug 2026, so anything reaching back past that date fills with rows whose reason was never recorded (58% of eligible declines at 30 days against 0% at 6, which inverts the headline). When it does dominate, stop reading reasons and **read the payloads against the gate's own expression**: the L30 gap path gates on `float(payload.get("importance") or 0.0) >= 0.55`, and all 13 invented hypothesis cues carried no `importance` key at all while all 13 grounded ones did — a missing key is 0.0, so half the shelf was structurally unreachable rather than unlucky (H7). Any gate that coerces a missing field to a falsy default will read "absent" and "worst possible" identically. |
| **She wonders things and none of them ever settle / hypotheses pile up and expire** | The second section of `python scripts/cue_reach_report.py`, which prints the L30 funnel — invented → asked → adjudicated → graduated — because the cue table above only answers whether she got the *chance* to wonder aloud. **Read the distances, not the per-status counts:** graduation needs `hypothesis_graduate_min_support` (2) confirmations plus `credence >= 0.7` on one row inside `hypothesis_ttl_hours` (336h), and no per-status total shows that a two-event bar sits inside a one-event window. Lifetime as of 19 Aug: 5 rows ever asked, **1 of 5 asks produced any verdict**, 10 rows exited by expiry, 1 refuted, and **0 have ever graduated** (H44). So expiry is not one exit among several, it is the exit. Two distinct failures wear the same symptom and the report separates them: `asked and unscored` rows mean the *resolver* never classified an answer that arrived (H7's remaining gap — she asked, he answered, nothing scored it), while a `parked at the bar` row means adjudication worked and the bar is the blocker. **Do not lower `min_support` to make a graduation happen**: the one row that reached `support_count = 1` has `asked_count = 0`, i.e. it was confirmed passively and never actually put to him, and a guess that was never asked and never contradicted is the worst possible candidate for becoming a durable belief about him ([shape 9](../docs/personality-backlog/health.md#recurring-shapes) — the gate may be the only thing containing an unmeasured producer). Fix the resolver, then re-read. This section exists because H7 ended a pass on "watch for the first non-zero `asked_count`"; it went non-zero eight days later and the entry said zero for another twelve ([shape 23](../docs/personality-backlog/health.md#recurring-shapes)). |
| **An always-on block is present every turn but nothing seems to respond to it** | It may be a constant. `turn_prompt_blocks` stores the rendered length per (turn, block), so `SELECT block, COUNT(DISTINCT assistant_message_id) n, COUNT(DISTINCT chars) d FROM turn_prompt_blocks GROUP BY block` finds every candidate at once: **`d = 1` over hundreds of turns means the block never changed**. Five such blocks are fixed grammars and are meant to be; the rest advertise themselves as learned. K13's `style_signal_block` sat at exactly 40 chars on 99.7% of 2018 turns, said one of three things ever, and changed four times in twelve weeks (H21). The cause is generic: **an absolute threshold applied to a stable trait is a constant, however well tuned** — three of its five axes had never emitted a label and could not (emoji bar 0.05 against a corpus maximum of 0.000, slang bar 0.15 against 0.009, question bar 0.40 against 0.333). For any "lately"/"currently" signal, check it compares against a baseline rather than a fixed number. Note `chars` is a *lower* bound on variation — equal lengths do not prove equal text — so it produces a shortlist to read the renderer for, not a verdict. |
| **A deviation / z-score signal fires on ~0% or ~100% of turns** | Two arithmetic mistakes, both of which produced a plausible-looking implementation. **Never fires**: you are dividing by the per-turn standard deviation when the tested quantity is a *mean of N samples* — the yardstick is the standard error `sd/sqrt(N)`. On a binary axis the per-turn spread is ~0.5 and no window mean is three of those from anything. **Always fires high for the first few months**: an EWMA seeded at 0.0 has a long cold start — at `alpha=1/300` it reaches only 49% of the true mean after 200 observations, so everything reads "above baseline". Floor the effective rate at `1/count` so it is an exact running mean until the decay horizon. Both bit K13 (H21) and both are covered by named regression tests in `tests/test_style_signal.py`. |
| **A prompt block never appears at all / "is this feature even alive?"** | The complement of the constant-block query, on the same table. `_PROMPT_BLOCK_TIERS` (`app/core/session/prompt_assembler.py`) is the declared inventory and `turn_prompt_blocks` is what reached the model, so the set difference is every block that has never emitted a character — 47 of 113 when this was first run. **Empty renders are dropped before the insert, so absent means "never rendered non-empty", not "never wired".** Two large decoys before you read anything into a name: (1) `grounding_line_mode == "replace"` deliberately zeroes ten T4/T5 ambient + affect blocks and fuses them into `grounding_block`, and (2) most T6 detectors (`rupture`, `boundary_clash`, `fact_reversal`, `user_correction`) *should* be silent over a short window — **rarity is the design intent for about half the list, so "never fired" is not evidence on its own.** The discriminator is never the block, it is the producer: read the worker's `idle_worker run done: <name> ... result={...}` payloads, which name their own skip reason. `candidates=6 ... drafted=0` on every run is a finding (H22); `no_finding` on a worker whose input is genuinely empty is not. |
| **"Is she deciding anything, or just being nudged?"** | `turn_stance` (K92) records one row per turn: `stance` is what the arbiter settled on, `desire` is what the providers offered, `ceiling` is the most floor-taking move the user's turn permitted, and `brevity` / `brevity_reason` (v37) are the orthogonal how-many-words axis. `SELECT stance, desire, COUNT(*) FROM turn_stance GROUP BY 1,2` is the whole diagnostic, or run [`scripts/lead_follow_report.py`](../scripts/lead_follow_report.py) for it beside the K90 metrics. **Read the disagreement, not the winner** — `WHERE desire <> stance` grouped by `reason` says how often she is being pushed to take the floor at a moment that cannot carry it (28.7% of turns, down from 36.9% before H39). A stance with a high `desire` count and a low `stance` count is oversupplied, not popular. **If one `reason` dominates its siblings, suspect the signal's timescale before its threshold**: `arc_protected` was 65% of clamps because `arc` is a conversation-level label whose runs average 17 turns and reach 110, so a per-turn veto on it muted her for days (H39) — the run-length query is the diagnostic, not the count. Unlike `turn_prompt_blocks` this table **is backfillable** — the arbiter is a pure function over blocks plus `messages` — so after changing a rule in [`stance.py`](../app/core/conversation/stance.py) re-run [`scripts/backfill_turn_stance.py`](../scripts/backfill_turn_stance.py) (it takes `--protected-arc-turns`, `--brevity-word-floor`, `--brevity-run` so a rule can be swept without editing code) and re-read rather than waiting for turns to accumulate. Only `FOLLOW`, `brevity` and K94 `sequencing` reach the prompt, via `stance_block`; every other rung is recorded but silent, so `rg "stance cue:" data/app.log` shows the subset that actually cost characters. **`sequencing` (v38) reads 0 on any row the post-turn path recomputed** — by then the style tracker holds this turn's reply, so the input would be circular rather than merely stale; re-run the backfill for history. **A recorded ceiling is not an enforced one, and the gap between them is a real bug class** — join the ledger against what actually rendered to find it: `SELECT COUNT(*) FROM turn_stance ts JOIN turn_prompt_blocks tb ON tb.assistant_message_id = ts.assistant_message_id WHERE tb.block='initiative_block' AND tb.chars>0 AND ts.reason='direct_question'` returned **17 of 75** because K53's gate walk could not see the ceiling and read only message length (K95, fixed). Run that shape for any block whose stance the ceiling caps; `planning` and `vent` are still recorded-and-unenforced by design. |
| **Her replies still open on his sentence ("That makes sense…", "Then those are yours")** | The anaphoric-opener rate, K88's number and the only honest one in the lead/follow family — it sat at 16-18% across every window through two families of persona rules aimed at it. Read it with [`scripts/lead_follow_report.py`](../scripts/lead_follow_report.py) (`anaph`, plus the opener histogram) rather than by reading turns; a habit at one-in-six is invisible in any sample small enough to read. **Two mechanisms speak to it and they must not both fire:** K88's `style_pattern_block` off a twelve-turn window (four hits in twelve, per-band cooldown) and K94's `sequencing` clause on `stance_block`, which fires when her *immediately previous* reply opened that way — the latter stands down when the former rendered, checked via the offer set the arbiter is already handed. `rg "stance cue:" data/app.log` shows `sequencing=anaphoric_run` on the turns it asked. Expect it near **6.4% of turns**, not `FOLLOW_AND_ADD`'s 46%: the cadence is the design, and it is self-extinguishing by construction, so a share drifting up means her openers got worse, not that the rule broke. If the rate does not move, the failure mode to check by hand is a reply that opens on her own footing and then *thins out the answer* — the clause says "answer him fully" precisely because that is the way it could do damage, and no counter will show it. |
| **`topic_miss` dominates the cue ledger / "she never brings up anything relevant"** | H43, and **do not treat this as a strict gate before measuring its acceptance rate**. `topic_relevant` is shared by five providers (`concept_hypothesis`, `curiosity_gradient`, `interest_drift`, `associative_wander`, `knowledge_gap_notice`) and was 1,873 of 1,981 eligible declines — while accepting **33.2% of every real (subject, message) pair**, which on a 0–5 cue shelf is an ~87% chance of matching something per turn. It is nearly a no-op, so it was never the decliner: that was a **cadence hold wearing its label**, because `take_pool_cue` inferred the reason from residual state and a hold strips the shelf *before* the predicate runs. Fixed — `pick_pool_cue` returns a `CuePick` with `considered` / `held_for_cadence` / `rejected`, and `topic_miss` is now only recorded when the predicate was the *only* refuser (deliberately an undercount, since `topic_miss` is eligible and `cadence_block` is not). **Expect `topic_miss` to fall sharply and `cadence_block` to rise by about as much; if it doesn't, the tie-break is wrong rather than the measurement.** The gate's *quality* was the real defect and is not fixed by tightening: verdicts it decided sit at cosine **0.370** against a measured null of **0.369** (chance), because **82% of its match-carrying tokens are function words** — `and` 39k, `the` 34k, `you` 26k, plus her own name. So the cosine now **ranks** the admitted set instead of vetoing it (acceptance unchanged, reach cannot fall; winner changes on 49% of `concept_hypothesis` and `curiosity_gradient` turns). `rg "cue pick:" data/app.log` shows `arm=` (`lexical` / `cosine`), the winning `cos=`, and `admitted=` — **`admitted=1` means ranking had no choice to make, so a thin shelf reads as "working" here.** Run [`scripts/topic_gate_report.py`](../scripts/topic_gate_report.py) before touching `cue_topic_min_cosine`; it prints the null first, because **a similarity threshold is meaningless until you know the score between pairs known to be unrelated**. Two traps that cost a day: shelf depth is **not** recoverable after the fact (`state`, `not_before`, `surfaced_count` are last-value-only, so `created_at <= T <= expires_at` overcounts wildly — the `cue pick empty` debug line is the only record), and any replay of past decisions needs an arm whose answer you know in advance (mine had to return ~0% on turns already declined; it returned 55–82%, which is how the reconstruction was caught). |
| **She takes the floor and spends it on something trivial** | K93. The scarce beats (K53's "this turn is yours", the imperative band, K54's offer) ask the wants ledger for one want, and until 19 Aug they asked for the **strongest** one — which is a clock, not a judgement: `pressure += growth_per_day * elapsed_days`. Read `get_wants_state`, which now lists wants in the order those beats actually pick, each with its `substance_tier` (3 anchored / 2 a question for him / 1 free association). Two reasons pressure could never promote substance, both worth knowing because the shape recurs wherever a score doubles as a rank: seeds are minted at **0.15** against a goal's 0.05 and a pursuit's 0.04 and then all grow at the same rate, so the ~0.11 lead **never closes** (a live seed at 0.60 and a goal at 0.50 were the same age to the day); and everything saturates at the 1.0 clamp inside four days against a 14-day TTL, after which `sorted` is stable and the tie-break silently becomes *ledger insertion order*, where the worker ingests seeds first. `rg "wants-ledger imperative fire:" data/app.log` now prints `tier=` beside the pick. If a beat has gone quiet instead of trivial, suspect the opposite mistake: `strongest_for_floor` **filters by the caller's pressure bar before ranking**, and reordering across that bar (K54's `appetite_min_want_pressure`, the imperative threshold) is what would turn "a better offer" into `no_offer`. `agent.wants_substance_ordering=false` restores pressure-only order for an A/B. |
| **She takes the floor right after he asked her something** | K95, and the gate is now in K53's walk: `rg "initiative-director: reason=" data/app.log` — `direct_question` means it deferred. **Deferred, not spent**: `turns_since_initiative` resets only on a real fire, so the directive lands on the next turn that is not a question and the beat is never lost (the `user_substantial` precedent). If you see `reason=fire` on a turn ending in a question mark, check `agent.initiative_respect_direct_question` (default on; it exists to A/B the placement change) and whether `force` was armed — the MCP repro tool deliberately bypasses this gate. The predicate is shared with K92's ceiling via [`turn_shape.py`](../app/core/conversation/turn_shape.py) and a test asserts *the same function* backs both, because the ledger saying `direct_question` while the prompt says "this turn is yours" is the exact failure being prevented. Note it reads the K4 act tag **or** a trailing question mark: the tag is stamped post-turn so it lags a turn, and OR-ing means a stale tag can only add a deferral, never miss one. |
| **An affect signal fires on turns with no emotional content (esp. greetings)** | Suspect the clock, not the model. `AffectUpdater.apply_turn` decays valence toward baseline for the elapsed gap **before** applying the reaction impulse, while `AffectStore.get` is a raw row read — so differencing a pre-turn snapshot against a post-turn value measures **how long the user was away**. At a 30-minute half-life, ~26 minutes from a warm goodbye clears K8's 0.12 rupture threshold with no emotional event at all, which is why all nine of J6's `repair` shared moments were reunion openers ("Where is my love?", "Good morning aiko") and one logged `drop=0.221` on a turn whose impulse was *positive* (H36). Read `rg "K8 rupture" data/app.log`: the line now carries `stored_prior` and `baseline` beside the decayed pair, and `stored_prior` far above `prior` **is** the gap. Two rules: decay any snapshot before comparing it to a post-`apply_turn` value (`AffectState.decayed()`), and gate "something bad happened" on the resting `baseline_valence` — a fall from unusually warm back to merely fine crosses any drop threshold and wounds nobody. Same shape for the recovery side: J6's `has_recovered` compared only against the dip floor, so a 0.10 rise off −0.50 certified "you talked it out and were okay after" at −0.40. |
| **She writes something odd and no prompt block asked her to** | Check what the *user's* stored turn looks like before you go looking for a persona line. The transcript replayed into the prompt is the post-`sanitize_user_text` copy, so anything that function rewrites is training data. Its punctuation whitelist deleted the `<` of an incoming `<3` and kept the digit, which put 230 turns of "I love you 3" in the history Aiko reads as her own, and she duly learned the digit as the way to write affection — twelve replies had copied it and TTS said one out loud as "Sleep well, Jacob. three" (H37). The tell is a one-liner: compare the `[ui] ws message` log line (raw, as typed) against the row in `messages` (sanitised) — `rg '"role": "user"' data/app.log` beside `SELECT content FROM messages WHERE id = ?`. If they differ, the difference is what she is being taught. Same query settles the reverse question: `llm raw response:` shows whether she emitted the broken form or something downstream broke it. **Never diagnose a habit from the transcript alone — the transcript is the input, not the evidence.** |
| **A worker reports the same "new" item on every single run** | It is being created and then discarded before the save, and the bookkeeping is reading the pre-discard value. K73 logged `new=['saturday:afternoon:casual_check_in']` on eight consecutive sweeps because `merge_rituals` collected `new_keys` *before* trimming to `max_active` — the key was new, trimmed, and new again forever (H22). Two rules fall out: **compute "what changed" from what survived, not from what was proposed**, and when a cap treats some rows as permanent, give the permanent set its own budget — `keep_pending = max(0, max_active - len(sticky))` reaches zero and terminates the feature the moment the sticky set fills the cap. |
| **A cosine / confidence / similarity threshold that never admits anything** | Plot the distribution before touching the constant; the bar is often outside the range the model can produce. `associative_wander` reported `no_pair` on 107 of 107 runs against a `max_pair_cosine` of 0.25, and the **minimum cosine over all 561 eligible pairs in the live graph was 0.2648** — it missed the corpus floor by 0.015 (H23). Sentence encoders put genuinely unrelated text at 0.3-0.5, not near zero, so a threshold like this encodes an intuition about cosine space that no real encoder satisfies, and it silently breaks again on any model swap. **Prefer a quantile of the observed pairs to a fixed constant** (keep the constant as a sanity ceiling), because a rank cannot be placed out of reach. Same root shape as H21: an absolute threshold is a claim about a distribution nobody measured. |
| **The app reopens the wrong conversation on launch** | **Ask what wrote the pointer before you debug the pointer.** `session.last_active_id` in `config/user.json` is honoured over the database by `_resolve_initial_session_id`, and the cause here twice turned out to be an *external* writer, not the logic: the test suite persists it through `_touch_last_active_session` whenever a test drives a controller through a turn, and it left the live install naming `main` (May) or `s2` (12 Aug), both real conversations (H45). The one-step diagnosis is to compare the file against what the code reads — `read_user_overrides()` in a fresh process versus the raw file. Two different answers means something rewrote it under you; a full test run is the first suspect, and `tests/conftest.py` now redirects the path plus fails the run if the live file changes. Only if they *agree* is it worth checking the pointer against ground truth (`SELECT session_id, COUNT(*), MAX(created_at) FROM messages GROUP BY session_id ORDER BY 3 DESC LIMIT 5`) and then the write path (`persist_user_overrides` swallows errors at debug level — raise `app.session` to DEBUG and look for `failed to persist last_active_id`). Note the empty-session case is deliberate: "New session" → close → reopen keeps you on the fresh empty one, so a pointer with no messages behind it is not evidence of a bug. |
| **A setting applies, survives a reload, and is gone after a restart** | The runtime setter mutated `self._settings` and never called `persist_user_overrides`. Nothing in the app will tell you: the REST GET reads the same in-memory object the setter wrote, so the drawer shows the new value and a browser refresh keeps showing it — only a process restart reveals that disk never heard about it. This was the TTS engine picker (H45); the file is the arbiter, so `rg '"provider"' config/user.json` after making the change is the whole test. When adding a setter to `voice_mixin` / `avatar_mixin` / the settings routes, persist in the same method that mutates, and prefer a **per-provider / per-entity** key over a flat one where the value is not portable between engines. |
| **The PWA opens on an empty chat that isn't empty** | History hydration is `useSessionHistory`, called from `App` so it runs on every layout. It used to live in `SessionSidebar`, which the desktop tree always mounts but the phone tree only mounts inside `MobileNavDrawer` — and that returns `null` while closed, so on a phone the socket connected, `hello` set a perfectly good `sessionKey`, and the transcript was simply never fetched. Tell the two apart from the browser console: if `sessionKey` is set and `GET /api/sessions/{key}/messages` never fires, it is the hydration path, not the session. Anything that renders the transcript belongs above the layout branch in `App.tsx`, never inside a conditionally-mounted panel. |
| **She greets you like a stranger at the start of a new conversation** | Expected before K91, and the reason it exists: a new `session_id` empties the transcript, blanks the (session-keyed) rolling summary and K21 thread note, and silences *every* gap cue at once — J5, K14, K28, H21, K36, K34 all measure from the previous assistant message **in the same session**, and a fresh session has none. Check the bridge is rendering: `SELECT chars FROM turn_prompt_blocks WHERE block='continuity_block' ORDER BY created_at DESC LIMIT 5`. Zero rows on the opening turn of a conversation means the gate closed — either `agent.continuity_max_messages` is `0`, this is the first conversation on record (nothing to bridge from), or the previous session's last `created_at` would not parse. A row with no "Where that thread stood" line just means that session never earned a K21 note, which is normal for short threads; the elapsed time is still carried. |
| **She talks about something upcoming that already happened (or asks about it again afterwards)** | Read the stored row before the prompt: `SELECT id, temporal_type, created_at, event_time, content FROM memories WHERE content LIKE '%<thing>%' ORDER BY created_at`. Two specific corruptions to look for, both fixed in H40 but present in rows written before 19 Aug. **(1) A plan filed as `past_event`.** The K-time10 backstop in `MemoryStore.add` read any relative time word as evidence of a past event, and five of the eighteen (`tomorrow`, `tonight`, `next week`, `soon`, `this weekend`) mean the opposite — so "the courier comes tomorrow" landed in the one lane with no upkeep: `MemoryDecayWorker` only moves rows *into* `past_event`, and the K-time3 "Coming up" block reads `future_plan` exclusively. The population query is the tell — `SELECT temporal_type, COUNT(*) FROM memories GROUP BY 1` showed **17 `future_plan` against 246 `past_event`**, and a companion who plans things should not have a 0.8% plan rate. **(2) `event_time` ahead of `created_at` on a `past_event`.** `humanize_past` used to answer `"moments ago"` for a timestamp it could not represent, so such a row read as brand-new for the whole gap — 54 rows, median 15 h, one for 188 h. Reproduce a bullet exactly as the model saw it with `timephrase.temporal_suffix(temporal_type=…, event_time=…, created_at=…, now=…)` rather than reasoning about it; if several rows from different days return the same tag, that flattening *is* the bug. Fresh writes are now guarded (future wording → `future_plan`, no invented `event_time`; a `past_event` dated after its own write is corrected), and `rg "dated after its own write" data/app.log` shows the correction firing. |
| **A filter rejects 100% of something — before you fix it** | Look at what it was rejecting. A broken gate is sometimes the only thing containing a broken producer: repairing L30's importance plumbing would have released three hypotheses asserting Aiko has a chassis, cooling fans and processing latency, straight into a lane whose output can graduate into her self-model (H7). `scripts/retire_machine_hypotheses.py` both cleans that up and demonstrates the pattern — reuse the *same predicate* for the cleanup and the new gate so they can never disagree about what counts. |
| **Proactive nudge fires wrong / never** | `tail_logs(module_contains="proactive")`. Check `source=prepared|llm` and the skip reasons (`cooldown`, `chat in progress`, `no history`). |
| **Aiko stopped using tools / never calls a tool she should** | P14 tool-pass gate may be skipping the decision pass. Call MCP `get_tool_gate_state` — a `last_decision.reason == "no_signal"` on a turn that *should* have used a tool means the signal-pattern table in [`app/core/session/tool_pass_gate.py`](../app/core/session/tool_pass_gate.py) is missing that phrasing: extend the matching family's patterns (or add the new tool to `_TOOL_FAMILY` — unmapped tools force always-run, so a missing mapping is never the cause of silence). Per-decision trace: `tail_logs(module_contains="tool_pass_gate")` shows one `tool-gate: run=… reason=… matched=…` line per turn; the `turn done:` line carries `tool_gate=` + `tool_pass_ms=`. One-shot repro: `force_tool_pass()` then `send_message(...)` should land `tool_gate=run:force`. Kill-switch: `agent.tool_pass_gate_enabled=false` restores the old always-run behaviour. Escalation ladder (option D small-model router, option B speculative stream) documented in [shipped.md → P14](../docs/personality-backlog/shipped/perf.md#p14-heuristic-tool-pass-gate--skip-the-forced-decision-pass-on-banter-turns). |
| **Aiko never follows up on her own "I'll look into that" / pretends she did** | K43 promise follow-through failure mode. Walk the gates: 1) Call MCP `get_promise_followthrough_state` — confirm `enabled=true` and look at `status_counts.assistant`. No `open` rows at all means the extractor never caught the phrasing — that is the LLM pass in [`app/core/memory/promise_worker.py`](../app/core/memory/promise_worker.py) plus its quality gate (`_is_low_quality`), not a regex; `promise_extractor.py` is only the value object now; rows stuck `open` with an old `last_fired_at` means the idle worker is starved (cross-check `get_idle_workers_status` for the `promise_followthrough` row) or the cooldown/age gates are blocking (`settings` in the same dump). 2) Repro end-to-end: make Aiko say "I'll look into X" → `force_promise_followthrough()` (bypasses age + cooldown) → `send_message(skip_tts=true)` → `tail_logs(module_contains="promise")` should show `promise-followthrough armed:` then `promise-followthrough fire:`, and the Heads-up line lands in `get_last_response_detail.system_prompt`. 3) If the cue fires but Aiko *invents* a result instead of owning that she hasn't gotten to it, that's a persona bug — read the "Things you said you'd do" block in [`data/persona/aiko_companion.txt`](../data/persona/aiko_companion.txt). 4) Promises never flip to `fulfilled`: the lexical matcher needs `memory.promise_fulfil_min_overlap` (default 3) shared content words between reply/task-result and the promise body — grep `promise fulfilled:` at INFO; lower the knob if real fulfilments are being missed. Full reference: [K43 in shipped.md](../docs/personality-backlog/shipped/patterns-k31-k60.md#k43-promise-follow-through--close-the-loop-on-ill-look-into-that). |
| **She asks after something he promised weeks ago / raises a promise she already kept / never notices one was missed** | The promise lane, and H41 found three separate leaks in it. Start with the shape of the store: `SELECT json_extract(metadata,'$.promise_who'), json_extract(metadata,'$.promise_status'), COUNT(*) FROM memories WHERE kind='promise' GROUP BY 1,2`. **(1) Every user row `open` and nothing else** is the pre-19-Aug state — 86 of 86, oldest 86 days. Retirement now runs on both sides in `PromiseFollowthroughWorker._should_retire`; *surfacing* is still assistant-only by design, so `open` user rows are expected, but ones older than `memory.promise_followthrough_drop_after_days` (14) with no deadline are not. **(2) A resolved promise being spoken** means the surface skipped `promise_status`: only the follow-through block and (since H41) `prepared_nudge` re-validate it, and RAG hides `dropped` but deliberately keeps `fulfilled`. Grep `promise-followthrough fire:` and `promise_followthrough silent:` — the latter is the re-validation working. **(3) Lateness never mentioned** is a missing deadline: check `json_extract(metadata,'$.promise_deadline')` is non-null and `rg "promise-worker upsert:" data/app.log` shows `deadline=` rather than `-`. Before H41 the deadline lived only as prose inside `content` (`"… (by 2026-08-19)"`) and nothing parsed it, so every decision ran on `created_at` — a promise due by lunch read as fresh all afternoon. Rows written before 19 Aug still carry it only in the sentence; `python scripts/backfill_promise_deadlines.py --dry-run` reports what it would lift out. `overdue_hours` is the axis to reason with, never `promise_age_hours`. Full reference: [H41 in health.md](../docs/personality-backlog/health.md). |
| **Slow first token** | Grep `first_token_ms=` over recent turns. If elevated, `set_log_level("app.llm.ollama_client", "DEBUG")` and watch `ollama chat_stream done:` for the per-call `elapsed_ms`. |
| **Wrong provider on a worker / chat path** | "Aiko's chat is hitting OpenAI but a worker is also burning my quota", or the opposite "Why is the chat path still talking to Ollama after I switched to OpenAI?". Call MCP `list_llm_providers` for the catalogue + `list_llm_routes` to see exactly which provider id each role resolves to. Mismatch between expected route and actual client: call `get_client_cache_stats` — each entry shows `provider_ids: [...]` (the providers sharing that slot) so you can confirm route N -> provider P -> cached client C. If a route's `provider_id` is right but the live `_chat_client` was built from a different one, the cascade fired pre-mirror: `set_log_level("app.session", "DEBUG")` and watch for `llm: updated route main_chat -> provider=…` on the next reconfigure. End-to-end retarget without UI: `set_llm_route(role="main_chat", provider_id="openai", model="gpt-5-mini", context_window=131072)` then send a message and verify the next `turn done:` INFO line shows the new `model=`. Full reference: [LLM provider catalogue + role mapping in code-conventions.md](code-conventions.md) and [docs/llm-providers.md](../docs/llm-providers.md). |
| **Low cache-hit rate on OpenAI (high per-turn cost)** | "I switched to `gpt-5-mini` but per-turn cost looks like the *uncached* column on the pricing page." First, confirm the symptom: `rg 'turn done:' data/app.log | rg -o 'cached_pct=[0-9.]+\|prompt=[0-9]+' \| tail -20` — healthy OpenAI sessions settle around `cached_pct=80.0` … `95.0` from turn 2 onward. Numbers stuck near `0.0` mean the prefix is being invalidated turn-to-turn. Step 1: confirm the provider actually returns the field — call `get_last_response_detail` after a turn and look at `usage.cached_tokens`; if it's missing entirely (not just zero), the upstream provider doesn't implement OpenAI prompt caching (Gemini / Groq / most OpenRouter routes — that's a "feature" of the provider, not a bug). Step 2: if cache is supported but cold, `set_log_level("app.prompt_assembler", "DEBUG")` and inspect the `prompt built:` line on two consecutive turns — diff the `providers=` count and `slowest_provider=`. The single most common cause is a per-turn block landing above a stable one (e.g. a new T6 detector got appended into the T1 cluster). Step 3: cross-reference the appended block with `_PROMPT_BLOCK_TIERS` in [`app/core/session/prompt_assembler.py`](../app/core/session/prompt_assembler.py) — if the block is in the wrong tier, move its `if block: system_parts.append(block)` line into the correct cluster (T0 → T6 strictly). Step 4: if the prefix looks right but cache is still missing, the cache may simply be cold — TTL is 5–10 min, so any silence longer than that triggers a full-price re-warm. Step 5: stop guessing which block broke it — set `logging.prompt_cache_log_enabled: true`, hold a normal conversation, and run `python scripts/prefix_break_report.py`. That names the earliest diverging block per turn, what it cost, and whether the provider's `cached_tokens` tracks our prediction (if it does not, the misses are the provider's routing or TTL, not our prompt). Step 6: read the report's **ladder discipline** section rather than fixing the earliest break and re-running. `diverged` names one block and hides every tier behind it, so a fix promotes an unknown successor; the ladder section ranks all tiers' change rates at once and flags an inverted pair (`INVERTED T0_stable (66.7%) churns more than T1_semi_stable (21.7%)`) plus the block filed higher than it behaves. An inversion means either the block is in the wrong tier or its text carries a per-turn-varying value — a monotonic counter or an unquantised float — which must never reach a T0–T2 block. Full reference: [`docs/prompt-caching.md`](../docs/prompt-caching.md) (tier ladder + contributor guide + P44 measurement). |
| **Slow turn — where is the time going?** | The `turn done:` INFO line carries `embed_ms=` (P1, sum of `/api/embeddings` round-trips on the turn thread), `assemble_ms=` (full `assemble_with_budget` body), and `rag_lookup_ms=` (prefetch + live RAG). For per-provider attribution flip `app.core.session.prompt_assembler` to DEBUG and read `slowest_provider=` from `prompt built:`, or call MCP `get_last_response_detail` and inspect the `provider_ms` dict / `embed_calls` / `embed_ms`. |
| **Compaction not triggering** | Grep `context overflow projected` and `compactions=`; if the threshold is wrong, look at `est_tokens=` / `budget=` from `prompt_assembler`. |
| **Scheduler jobs queued but not running** | `set_log_level("app.scheduler", "DEBUG")`, watch `scheduler drain:` for `jobs_run=` and `queue_after=`. |
| **Idle workers starving (decay/promotion/etc. never run)** | Call MCP `get_idle_workers_status` — sorted most-overdue first. The header confirms whether `quiet` is `false` (the gate is blocking everything) or the per-tick `tick_budget_ms` is too small for the EMA estimates to fit. Per-row `overdue_seconds`, `avg_duration_ms`, and `error_count` triangulate "stuck waiting", "always slower than the budget allows", and "raising every run". Then `set_log_level("app.idle_worker_scheduler", "INFO")` and read `idle_workers tick: ran=N due=M skipped_budget=K names=…` per tick to watch the drain. Bump `memory.idle_worker_tick_budget_ms` (default 3000) for longer quiet windows; set `memory.idle_worker_max_per_tick=1` to reproduce the legacy one-per-tick behaviour. |
| **Aiko misreads timing / collapses future plans into past events** | Per-message wall-clock prefixes (K-time1) are gated by `agent.history_age_prefix_enabled` (default `true`). When ON, every history message arrives at the LLM as `[2 min ago] ...` / `[just now] ...` / `[yesterday 18:45] ...`. When OFF, the history block is byte-identical to the pre-K-time1 behaviour. Verify by setting `set_log_level("app.core.session.prompt_assembler", "DEBUG")` and re-running a turn — the rendered prompt's history messages start with `[…]`. The persona block "Wall-clock awareness in the conversation" in [`data/persona/aiko_companion.txt`](../data/persona/aiko_companion.txt) teaches Aiko to use the prefix as a clock without quoting it back. Full reference: [K-time1 in shipped.md](../docs/personality-backlog/shipped/patterns-k01-k15.md#k-time1-wall-clock-prefixes-on-chat-history). |
| **Aiko miscounts the days to an upcoming event / never mentions something coming up** | K-time3 upcoming-horizon block is gated by `agent.upcoming_horizon_enabled` (default `true`). A live (no-worker) forward sweep over `future_plan` memories due within `memory.upcoming_horizon_days` (default 7) renders one terse "Coming up for {name}: …" cue with the relative time **pre-resolved** by `timephrase.humanize_future`, so the model never recomputes a date. Walk the gates: 1) Call MCP `get_upcoming_horizon_state` — the dry-run `upcoming` list shows every `future_plan` in the window with its `resolved` phrase + the live `current_signature`. Empty means no `future_plan` row has an `event_time` in `(now, now+horizon]` (check the MemoryExtractor parsed one — `temporal_type='future_plan'` with a non-null `event_time`). 2) If the dry-run is non-empty but the cue never lands, it's the anti-nag gate: `cooldown_remaining > 0` with an unchanged `last_signature` suppresses an already-surfaced set — a new/passed plan re-surfaces immediately. 3) Repro end-to-end: `force_upcoming_horizon_surface()` then `send_message(skip_tts=true)` and confirm the "Coming up …" line lands in `get_last_response_detail`'s system_prompt. Grep `tail_logs(module_contains="inner_life")` for `upcoming-horizon fire:`. Pairs with the `follow_up_worker` (the retrospective "how did it go?" half once an event passes). Full reference: [K-time3 in awareness.md](../docs/personality-backlog/shipped/awareness.md#k-time3-upcoming-horizon-block--pre-computed-future-relative-times--shipped). |
| **Aiko never notices a long sitting / a mid-session pause (or nags about the clock every turn)** | K-time4 session-clock block is gated by `agent.session_clock_enabled` (default `true`). A live (no-worker) signal off the recent-message timestamps surfaces two one-shot sub-cues: **elapsed** (current *continuous sitting* duration, banded `long` ≥ `session_clock_long_minutes`/60 and `very_long` ≥ `session_clock_very_long_minutes`/150) and **pause** (a mid-session gap in `[session_clock_gap_min_minutes, session_clock_gap_max_minutes)` = `[10, 30)` min, capped at the K14 absence_curiosity floor so it never double-fires with the gap-return family). Walk the gates: 1) Call MCP `get_session_clock_state` — the `measure` block dry-runs the live signal (`elapsed_seconds` / `elapsed_band` / `gap_seconds` / `gap_notable`); the watermarks (`burst_key`, `fired_band`, `gap_anchor`) show what's already been surfaced this sitting. `elapsed_band=null` with a long real sitting usually means a gap > `session_clock_break_minutes` reset the burst (a fresh sitting started). 2) "Nags every turn" → the per-band/per-pause watermark should suppress repeats; if it doesn't, check `fired_band` isn't being reset (a changing `burst_key` re-arms it — i.e. messages keep landing > break apart). 3) Repro end-to-end: `force_session_clock_surface()` then `send_message(skip_tts=true)` and confirm the "been talking for …" / "was away …" line lands in `get_last_response_detail`'s system_prompt (a live band/pause must still actually hold). Grep `read_log_file(grep="session-clock fire:")` (the line is logged on the `app.session` logger). Full reference: [K-time4 in awareness.md](../docs/personality-backlog/shipped/awareness.md#k-time4-session-elapsed--mid-session-gap-awareness--shipped). |
| **Aiko keeps pushing when {user} goes quiet on her** | K23 subtle-misattunement detector is gated by `agent.misattunement_detection_enabled` (default `true`). Fires `mild_disengagement` when {user}'s reply is very short after a substantial Aiko reply (shrink trigger) OR is a short pivot away that K6 flags as `strong_novelty` (pivot trigger). Cooldown of 3 turns prevents stacking. End-to-end repro: call MCP `force_misattunement()` then send Aiko a short message ("ok") right after a long Aiko reply — the next prompt should include "Heads-up: {user} just gave a short reply..." and Aiko should pull back without apology-spiral language. Persona text lives in the "When {user_name} goes quiet on you" block of [`data/persona/aiko_companion.txt`](../data/persona/aiko_companion.txt). For per-fire diagnostics: `tail_logs(module_contains="misattunement")` shows `misattunement-detector: trigger=… prev_aiko=… this_user=… novelty_band=… cooldown_set=…`. Full reference: [K23 in shipped.md](../docs/personality-backlog/shipped/patterns-k16-k30.md#k23-subtle-misattunement-detection). |
| **Aiko quotes a 6-month-old claim as if it were yesterday** | K25 confidence time-decay is gated by `agent.confidence_time_decay_enabled` (default `true`) plus three `memory.confidence_decay_*` knobs (horizon, floor, distant_threshold). At defaults a default-confidence (0.7) claim crosses into `(distant)` territory at ~104 days; pinned rows always bypass. End-to-end repro: call MCP `get_confidence_decay_state(limit=50)`, find a row with `age_days > 150` and `stored_confidence >= 0.7`, confirm `effective_confidence < 0.5` and `distant=true`, send Aiko a message that should retrieve it — her reply should hedge with time-language ("a while back", "don't quote me on the date") rather than quoting it fresh. Persona guidance lives in the "Memories tagged `(distant)`" bullet of [`data/persona/aiko_companion.txt`](../data/persona/aiko_companion.txt). Full reference: [K25 in shipped.md](../docs/personality-backlog/shipped/patterns-k16-k30.md#k25-memory-confidence-time-decay). |
| **Aiko lectures the user instead of sharing her own taste** | K29 opinion injection failure mode: the detector fired correctly (stored stance contradicts a {user_name} claim) but Aiko ends up moralizing ("you should quit smoking, it's bad for you") rather than owning her preference ("ugh, smoke and I really don't get along"). This is a persona-block bug, not a detector bug — read the "When you have your own take" section of [`data/persona/aiko_companion.txt`](../data/persona/aiko_companion.txt). The block carries concrete bad/good pairs for the lifestyle (smoking / horror / late-night) failure mode and explicitly forbids "you should..." constructions. Verify the cue fired (the cause): `tail_logs(module_contains="opinion")` should show `opinion-injection fire: trigger=…` on the offending turn; then `get_opinion_injection_state` shows the matched stance text the LLM was reading. If the cue fired and the persona block looks right, try `agent.opinion_injection_require_definite=true` to gate down the LLM borderline path while you tune. Full reference: [K29 in shipped.md](../docs/personality-backlog/shipped/patterns-k16-k30.md#k29-opinion-injection--push-back-when-she-has-a-stance). |
| **Aiko never disagrees with the user, even when she has a stored stance** | The inverse K29 failure: the persona claims "have opinions" but the LLM smooths into agreement on a topic where Aiko has a real `kind="self"` stance memory that contradicts. End-to-end check: 1) Confirm `agent.opinion_injection_enabled=true` and at least one opinion-shaped stance memory exists (`POST /api/memories` with `kind="self"` + content like "I really don't like X"). 2) Send a contradicting message and watch `tail_logs(module_contains="opinion")` — if no `opinion-injection fire:` line shows up, walk the gates: master switch / cooldown / per-session cap / predicate filter / cosine threshold / heuristic verdict / LLM gate. `get_opinion_injection_state` dumps every relevant value. Common causes: (a) the stance memory isn't opinion-shaped (predicate filter drops it — biographical facts don't count), (b) cosine drops below `memory.opinion_injection_min_cosine=0.55` because the topical match is weak, (c) the heuristic returns `borderline` and the LLM gate said `NO` (this is intentional conservatism; check the borderline rate-limiter budget with `get_opinion_injection_state.llm_budget`). Use `force_opinion_injection()` to verify the wiring end-to-end. Full reference: [K29 in shipped.md](../docs/personality-backlog/shipped/patterns-k16-k30.md#k29-opinion-injection--push-back-when-she-has-a-stance). |
| **Aiko opens a returning conversation flat — never mentions she was thinking about anything** | K28 turning-over failure mode: the user came back after a long gap and Aiko's first reply reads like she went dormant between sessions. Walk the gates: 1) Confirm `agent.turning_over_enabled=true`. 2) Confirm the gap was typed AND at least 90 min (`memory.turning_over_min_gap_minutes`); voice mode never arms K28. 3) Call `get_turning_over_state` — the `would_surface` dry-run picker result tells you whether ANY reflection clears the gates. If `reflections_in_window=0`, there's no recent `kind="reflection"` row in `[24h, 72h]` — the ReflectionWorker / DreamWorker hasn't run recently OR the rows decayed out. If `reflections_in_window>0` but `would_surface=null`, the topical-similarity gate is rejecting every candidate (the recent reflection isn't on-topic with the live thread). Either insert a real reflection (`POST /api/memories` with `kind=reflection`, embedding aligned to an active goal, `created_at` 30h ago) or lower `memory.turning_over_min_topical_similarity`. 4) Call `force_turning_over()` then send a message; `tail_logs(module_contains="turning_over")` should show `turning-over fire: memory_id=… age_h=… topical=… source=… dream=…`. If the cue fires but Aiko still arrives blank, that's a persona-block bug — read the "What I've been turning over" section of [`data/persona/aiko_companion.txt`](../data/persona/aiko_companion.txt). Full reference: [K28 in shipped.md](../docs/personality-backlog/shipped/patterns-k16-k30.md#k28-what-ive-been-turning-over--between-session-thought-thread). |
| **Aiko keeps agreeing with everything / goes flat for a stretch / keeps restating the same point** | K30 self-noticing failure mode — Aiko's three "patterns I'm in" sub-detectors aren't firing OR Aiko is ignoring the cue. Walk the gates: 1) Confirm `agent.self_noticing_enabled=true` and the specific sub-switch is on (`self_noticing_agreement_streak_enabled` / `_flat_affect_enabled` / `_repeated_thought_enabled`). 2) Call MCP `get_self_noticing_state` — for agreement-streak look at `last_verdict.agreement_share` (should be `>= 0.80` for fire) and `pushback_share` (`<= max_pushback / sample_size`); for flat-affect look at `valence_range` / `arousal_range` (both should sit `<= 0.10`) and `notable_reaction_count` (must be `0`); for repeated-thought the post-turn ring should be size 3 and `last_cosine >= 0.85` should arm `flagged_for_next_turn`. 3) If the verdicts look right but the cue doesn't render, the cooldown is probably blocking — `cooldown_remaining` decrements per provider call. 4) Use `force_agreement_streak()` / `force_flat_affect()` / `force_repeated_thought()` for end-to-end repro: each is a one-shot bypass that surfaces the Heads-up unconditionally on the next turn. Verify the line lands via `get_last_response_detail` → `system_prompt`. If the cue fires but Aiko still ignores it, that's a persona-block bug — read the "Style patterns I'm in" section of [`data/persona/aiko_companion.txt`](../data/persona/aiko_companion.txt). Full reference: [K30 in shipped.md](../docs/personality-backlog/shipped/patterns-k16-k30.md#k30-self-noticing-cues--agreement-streak--flat-affect--repeated-thought). |
| **Aiko reads the same all day every day / never picks up a different colour after midnight** | K27 day-color failure mode — the daily personality colour isn't rolling. Walk the gates: 1) Call MCP `get_day_color_state` — confirm `enabled=true` and read the current `name` / `set_at` / `age_hours` / `is_stale`. If `is_stale=true` and the next provider call doesn't fire, the lazy fallback is broken (check `tail_logs(module_contains="day_color")` for `day_color lazy-roll:` lines or exceptions). 2) If `is_stale=false` but `set_at` was yesterday's local date, the time-zone math is wrong — verify the runner's local offset matches the user's. 3) The canonical worker fires hourly from the idle scheduler; gated by the quiet-window predicate (`session._is_user_idle`). Long live-mode sessions can starve it — call MCP `get_idle_workers_status` and look for the `day_color` row's `last_run_at` / `overdue_seconds`. The provider's lazy fallback is the seatbelt: even when the worker is starved, the *next* turn after midnight reads `is_stale=true` and rolls in-line. 4) Repro end-to-end without waiting for midnight: call `force_day_color(color="pensive")` then `send_message(skip_tts=true)` — verify the "Your day's colour today: pensive --" line lands in the prompt via `get_last_response_detail.system_prompt`. Then call `reroll_day_color()` and confirm `get_day_color_state` shows a fresh `set_at`. If the line never lands in the prompt despite the worker / kv state being correct, check `agent.day_color_enabled` and the K16 suppression matrix (K27 is a trend/phase block and must NOT be in any drop list). Full reference: [K27 in shipped.md](../docs/personality-backlog/shipped/patterns-k16-k30.md#k27-aikos-day--daily-personality-colour). |
| **Aiko keeps oversharing in soft moments / never seems to pace her disclosures** | K15 vulnerability-budget failure mode — the rolling token-bucket isn't surfacing the cue or Aiko isn't reading it. Walk the gates: 1) Call MCP `get_vulnerability_budget_state` — confirm `enabled=true`, read the persisted `spent` + `last_decay_at`, the live `closeness` / `trust` axes, the computed `capacity`, the `ratio`, and the `cue_preview` field. If `cue_preview=null` and `ratio < 0.5`, the budget is healthy — that's the *correct* silent state for a fresh / well-paced session. 2) If `spent` is high but `cue_preview=null`, the relationship axes are inflated (closeness + trust both near +1 -> capacity 12 -> a few tier-3 spends still register as healthy); cross-check via the Together tab. 3) Repro the cue end-to-end without writing a real personal note: call `spend_vulnerability(tier=3)` twice (lands `spent_after=12`, `ratio=1.0`, at-cap cue), then `send_message(skip_tts=true)` and verify the "You're at the soft-moment cap with..." line lands in `get_last_response_detail.system_prompt`. 4) If the post-turn writer isn't firing on real self-tags: grep `tail_logs(module_contains="post_turn")` for `vulnerability-budget spend:` lines after a message that should have emitted one. Missing log = `[[remember:self:...]]` wasn't actually emitted (check the raw assistant text in `get_last_response_detail`) OR the master switch is off OR the body classified at tier 1 with cost 0. 5) Reset between tests: `reset_vulnerability_budget()` arms a one-shot kv wipe; the next provider call writes `BudgetState(spent=0)` and the budget is healthy again. 6) If the cue lands in the prompt but Aiko ignores it, the persona block in [`aiko_companion.txt`](../data/persona/aiko_companion.txt)'s "Sharing yourself" section is the read-side teacher — verify it's intact (the section should be ~8 bullets after "Your day's colour today"). Full reference: [K15 in shipped.md](../docs/personality-backlog/shipped/patterns-k01-k15.md#k15-self-disclosure--vulnerability-budget). |
| **Tuning the K16 grounding line / testing companion-feel** | The unified ambient grounding line is gated by `agent.grounding_line_mode`. **`off`** (default): no fused line, every granular block (circadian / world / activity / affect / mood_hint / relationship / user_state / ambient_noise) renders as before. **`replace`**: fused line replaces all eight granular ambient blocks. **`split`**: fused line replaces situational blocks (circadian, world, activity, ambient_noise) but keeps the trend/phase blocks (affect, mood_hint, relationship, user_state) standalone. Flip in `config/default.json` and restart, or call `session._prompt_assembler.set_grounding_line_mode("replace")` over MCP for a live switch. Verify by calling `get_last_response_detail` after a turn — `provider_ms.grounding_line` should be a small positive number in `replace`/`split`, missing or zero in `off`. The DEBUG `prompt built:` line's `providers=` count drops by the number of suppressed blocks. Full reference + suppression matrix: [`docs/personality-backlog/shipped.md`](../docs/personality-backlog/shipped/patterns-k16-k30.md#k16-unified-ambient-grounding-line). |
| **A time-gated feature "never fires" and you'd have to wait days to find out why** | Don't wait — time-travel instead. Restart with `AIKO_DEBUG_CLOCK=1` (against a **copy** of `data/chat_sessions.db`) and drive the DT1 virtual clock over MCP. See §f below for which of the two clocks you need. |
| **Time-gated behaviour is misbehaving for no apparent reason** | Check for a forgotten clock offset first: `get_status` carries a `debug_clock` block whenever the clock is shifted, and every advance logs at WARNING. `reset_clock` returns to real time. A stale offset makes anniversaries, cooldowns and decay all look independently broken. |
| **Crash / unhandled exception (backend)** | Read `data/crashlog.txt` (separate from `app.log`, only fatal traces + faulthandler dumps). |
| **The UI went blank / a panel is broken / "it crashed on my phone"** | Call MCP `get_ui_crashes()` — never read the raw stack first. Frontend crashes (error-boundary catches, uncaught window errors, unhandled rejections) are POSTed to `/api/logs/ui-crash` **always**, with no toggle to have forgotten, and land in `crashlog.txt`. Read the entry in this order: **1)** `breadcrumbs` — oldest-first with `+Nms` offsets, and it includes everything React wrote to `console.error`, which is usually the actual explanation (a failed fetch, a hook-order warning) sitting right before the throw. **2)** `context` — `build`, viewport, `shell`, `heapPct`, plus socket state / voice mode / open route at the moment it died. **3)** `stack_mapped`, the de-minified stack. If `stack_mapped` is **missing** while `stack` is full of `index-*.js`, the sourcemaps didn't resolve: `web/dist` was rebuilt after the crash (hashes no longer match — nothing to do but reproduce), or the build predates `build.sourcemap` in `web/vite.config.ts`. See §g. |
| **A concept threshold isn't the value in `config/user.json` / `memory_settings.py`** | Expected: the L45 tuner calibrates the read-side concept bars against the live distribution, so the shipped constant is a *default*, not the runtime value. Call MCP `get_gate_tuning` — per gate it shows the solved `value`, the `objective` and `why`, the unclamped `raw`, which rail had the last word (`clamped_by`), the distribution `stats` behind it, and `live` (what the running settings object actually holds). A gate marked `applied` whose `live` value differs means the apply path missed it; a gate showing `unapplied_because` naming `user.json` is your own override winning, which is correct. `clamped_by=floor` on every run means the *spec* is wrong, not the data. Most gates are deliberately `observe` (recorded, never applied) — those are the ones that write to the store. Force a run with `force_gate_tuning`; offline, `python scripts/concept_gate_report.py` dry-runs the whole registry against `data/chat_sessions.db` without the app and writes nothing, and `--trend` reads the history. Files: `data/tuning/concept_gates.json` (values + stats) and `data/tuning/concept_population.jsonl` (one graph snapshot per run, with `hours_since_previous` — the app is not always on, so don't read the lines as daily). Per-run log line: `tail_logs(module_contains="gate_tuning")` → `concept_gate_tuning run:` with `concepts=`, `moved=`, `applied=`, `duration_ms=`. Design: **L45** in [`docs/personality-backlog/concepts.md`](../docs/personality-backlog/concepts.md). |
| **Near-duplicate concepts pile up / consolidation never merges anything** | The L2 worker is LLM-budgeted, so the usual cause is starvation rather than a wrong threshold. Read the per-run INFO line first: `tail_logs(module_contains="consolidation")` → `concept_consolidation run: scanned=… pairs=… auto_merged=… adjudicated=… merged=… rate_limited=… duration_ms=…`. **`rate_limited=True` on every run with `pairs` far above `adjudicated`** means the backlog exceeds the daily budget — check `kv_meta['concept_consolidation.rate_state']` (`SELECT value FROM kv_meta WHERE key='concept_consolidation.rate_state'`) for `hour_used` / `day_used` against `agent.concept_consolidation_per_hour_cap` (6) / `per_day_cap` (30), and note the bucket keys are UTC, so a budget spent overnight reads as denied for the rest of the day. **`pairs=0` with obvious twins in the graph** means the pairs are under `memory.concept_consolidation_merge_cosine` (0.84) — verify with `python scripts/concept_gate_report.py`, whose `pair_cosine` population shows the live distribution. **`auto_merged` always 0** is expected — `concept_consolidation_auto_merge_cosine` ships at `1.0` (off), because on real labels most pairs above `DEDUPE_COS` are template collisions and a zero-LLM merge there destroys a belief rather than fusing a twin. Everything must go through the adjudicator, so budget is the only lever. A pair the LLM rejected is remembered for 30 days in `kv_meta['concept_consolidation.verdicts']`, keyed on the pair plus a digest of both labels — so if you expect a re-adjudication and get none, that map is why, and relabelling either concept invalidates the entry. Design: **L46** in [`docs/personality-backlog/concepts.md`](../docs/personality-backlog/concepts.md). |
| **The dormant pool only grows / nothing ever retires** | Two routes out of `dormant` and they fail differently. Confidence (`memory.concept_retire_confidence_floor`, 0.15) is the slow one: the L22 sweep demotes never-reinforced actives while their confidence is still high, and decay from there needs ~19 *engaged* days, so a month of calendar time with the app half-off moves almost nothing — that is expected, not broken. The duration route (`memory.concept_dormant_ttl_days`, 30 wall-clock days since `last_reinforced_at`) is what should be draining it; `0` disables it. Check the shape of the pool with `SELECT COUNT(1), ROUND(AVG(confidence),2) FROM concepts WHERE status='dormant'` plus `python scripts/concept_gate_report.py`, whose observe-only `concept_dormant_ttl_days` gate reports the live `dormant_quiet_days` distribution (n, median, max). Retirements land as `retired` events, so `tail_logs(module_contains="concept_lifecycle")` and the timeline both show them. Remember `retired` is revivable — fresh evidence brings a row back — so an over-aggressive TTL is not a data-loss bug. To simulate: interleave `advance_engagement(3)` with `force_concept_lifecycle` (see §f). |
| **A concept swallowed evidence that isn't about it / a concept stopped taking evidence** | The L31 admission gate refuses cited sources at reinforce time, in two ways that need telling apart. Read the per-pass line first: `tail_logs(module_contains="concept_synthesis")` → `evidence admission: admitted=… refused_offtopic=… refused_ceiling=… floor=… ceiling=…` (logged only when something was refused), then the DEBUG line per off-topic refusal, which carries the measured cosine, the source, and the concept it was cited for. **`refused_offtopic` climbing on evidence you think belongs** means `memory.concept_evidence_admission_cosine` (0.35) is too high for your labels — check the shape of your own inflow with `python scripts/concept_gate_report.py`, whose observe-only `concept_evidence_admission_cosine` gate reports the live `evidence_fit` distribution once the rolling sample has values (like every unmeasurable population it is absent from the report until then, so a fresh install shows nothing rather than zeroes); the shipped bar refuses ~2% and `0.45` refuses ~15%. **`refused_ceiling`** is `memory.concept_evidence_max_sources` (24) binding, which is only interesting if it fires on a concept you consider focused — confidence saturates at 8 sources, so a capped row loses nothing, and it *does* still bump `last_reinforced_at` so the L46 dormancy TTL can't retire it out the side door. **Nothing logged at all while a concept visibly accretes** means the gate never judged: it fails open when a source has no resolvable embedding (`("cluster", rep)` resolves through the rep's *memory* row, not `topic_clusters`) or when the concept itself is unembedded — check `SELECT dim FROM concepts WHERE id=…` and the memory's `embedding`. Both bars are forward-only, so an already-bloated row keeps its history by design; `SELECT id, distinct_source_count, label FROM concepts ORDER BY 2 DESC LIMIT 10` shows what predates the gate. Design + measurement: [`concept_evidence_admission.py`](../app/core/concepts/concept_evidence_admission.py). |
| **Aiko never tests a hunch / the invented-hypothesis lane went silent** | Read `get_hypothesis_state` first and compare `live` against `max_open` (12). **`live == max_open` is the whole story most of the time**, and there are two flavours: a high `linked` count means every row is already spoken for by a concept and will never surface, while everything sitting at `asked_count > 0` with a null `last_tested_at` means the rows think they were asked. The second used to be a permanent deadlock — the ask was spent when the cue was *published* rather than put, so a cue that never rendered still burned it, leaving rows un-re-askable (`asked_count <= 0` filter) and un-expirable at once. Fixed in two places: `SessionController._stamp_hypothesis_ask` moves the counter on the real ask, and `expire_stale` now exempts *answered* rows rather than asked ones. **There is a third flavour and it looks identical from the log** — `skipped: max_open` on a shelf that is simply *stale*: rows well short of the 336h TTL, nothing linked, nothing asked, so every gate is behaving and invention has still stopped for up to a fortnight (H42, measured at 7 days silent with 7.4 to go, 9 of 12 never asked). Tell them apart with the ages, not the count: `SELECT id, status, asked_count, linked_concept_id, created_at FROM hypotheses WHERE status IN ('open','supported') ORDER BY created_at` — a spread of 150–280h against a 336h TTL *is* the stale case. It should now self-clear: the proposer gives up its stalest never-asked row rather than refusing, logged as `hypothesis shelf full but stale: live=… replacing up to …` then `hypothesis evicted to make room: id=… guess=…`, with `evicted` in the run result. If those lines are absent on a stale shelf, check `hypothesis_evict_when_full` and whether the oldest row clears `hypothesis_evict_min_age_hours` (168h, half the TTL). `demand` now separates the cases too — `shelf_stale` means replaceable, `shelf_full` means genuinely fresh. Next, check throughput: `SELECT state, surfaced_count, COUNT(1) FROM cue_pool WHERE cue_type='concept_hypothesis' GROUP BY 1,2` — many `pending` rows at `surfaced_count=0` just means the producer outruns the 20h `surface_cooldown_hours`, and they will TTL out harmlessly. If the shelf is healthy and the lane is *still* quiet, the remaining suspect is the ask lane's topical gate: `topic_miss` was 382 of 444 declines in the week before H42, which is the H7 remainder and is not fixed. Design + the measured numbers: [`docs/hypotheses.md`](../docs/hypotheses.md). |
| **A capped pool has gone quiet and the log says it is full** | The generalisation of the row above, and shape 19 in [`health.md`](../docs/personality-backlog/health.md). `skipped: max_open` / `shelf_full` / `at_cap` are what a *correctly* full pool says, so the line is honest and useless. Do not read the count — **list the cap's exits and measure each one's throughput**. H42's shelf had two (asked-and-answered, or a 336h TTL); the first was declined `topic_miss` on 382 of 444 chances, so the TTL was the only working drain and the cap became a fortnightly duty cycle rather than a limit. The tell is one exit accounting for nearly all the drain, and each individual decline looking correct — declining a cue on a topic mismatch *is* right per-decision, and is only wrong as a rate. Where you can, prefer a cap that replaces over one that refuses: refusing is silent by construction, whereas replacing has to choose and a choice can be logged. |
| **You are about to hand-write a query against a table a doc already warns you about** | Don't; find the script, and if there isn't one, that is the bug. Shape 20 in [`health.md`](../docs/personality-backlog/health.md): a measurement mistake that is diagnosed, fixed and documented **in prose** gets made again by the next reader, who had read the warning — because the wrong number stays the cheapest thing to compute. The cue lane did this three times in a week (raw denominator → fixed denominator but undefined reach read as low reach → the answer sitting in a code comment the whole time). Two tells that a warning will not hold: it is phrased as a preference (*prefer the eligible rate*, so the wrong one is one `GROUP BY` away and the right one needs a join), and the correct computation lives somewhere the asker cannot reach — `get_cue_outcomes` needs a running app, while the question always comes up in offline forensics. So: **twice wrong means ship the instrument, in the place the question is asked, importing the production predicate instead of restating it.** Then print the *verdict*, not the input, wherever one is computable — `2 of 452` invites a conclusion, `+78.5h remaining of a 240h cooldown, by design` forecloses one, and ranking an undefined value as `0.0%` is the same error in miniature. |
| **She's dressed for the wrong time of day (pajamas at noon, day clothes at 3am)** | Circadian is the *documented* outfit driver, which makes it the usual suspect and often the wrong one. Read the two fields together before anything else — every `mood_state` WS frame carries both: `tail_logs()` and grep for `circadian_period` next to `resolved_outfit`. **Period correct but outfit wrong** means something set the sticky override, and there are two writers. Aiko's own `[[outfit:X]]` tag shows up as `llm tags: … outfit=Y` on the turn that set it; **if every turn says `outfit=n`, it was the weather feed** (`_apply_weather_seasonal_decor` nudges toward pajamas on a cold sky through the same channel). Grep `weather seasonal decor:` for the deciding line — it prints `blanket=`, `condition=` and `temp_c=`, so an implausible reading is visible immediately (H38 was a fabricated `temp_c=0.0` in August, from an Open-Meteo `current` block with no `temperature_2m`). The withdrawal is logged too (`weather outfit nudge withdrawn:`). **Period itself wrong** is a clock problem, not an outfit one — check for a stale `AIKO_DEBUG_CLOCK` offset per the row below. Remember the override is scoped to the period it was set in, so it also self-clears at the next boundary; a wardrobe that fixes itself on the hour was never a rendering bug. Full reference: **H38** in [`health.md`](../docs/personality-backlog/health.md). |
| **Wrong expression / cry-cascade / lip-sync glitch** | Enable **Settings drawer → Chat → Diagnostics → Debug logging**, reproduce, then grep `data/app.log` for `[ui]` lines. The browser pushes WS events + avatar-channel decisions (`channel.expression applyReaction reaction=… expression=…`) into the same file as the backend timestamps so cause + effect sit side-by-side. See [B6 in personality-backlog/shipped.md](../docs/personality-backlog/shipped/avatar.md#b6-ui-debug-logging-bridge). |
| **Touch gesture didn't fire / persona banner didn't appear / reaction didn't bump axes** | K31 + K32 failure mode — three independent half-paths to walk. **K31 dispatch path**: 1) Call MCP `get_touch_state()` — confirm `enabled=true` and read the per-kind `last_dispatched_monotonic` + `daily_counts` + `daily_date`. If `daily_counts[<kind>] >= cap` for the kind Aiko was trying to use, the dispatch was correctly gated. 2) Force the path bypassing every gate: `send_touch(kind="hug")` returns the dispatched payload — verify the WS `avatar_touch` frame fires (browser devtools → Network → WS → look for `"type":"avatar_touch","kind":"hug"`), the `ReachChannel` lean-in animates in both windows, the chat bubble badge `🫂 Aiko gave you a hug` renders, and the `PersonaActionBanner` appears in the open persona window. If the WS frame lands but no badge / banner — frontend wiring is the suspect; look at `web/src/store.ts` `pushAvatarTouch` + `appendGestureToCurrentTurn` reducers in the React DevTools. 3) Real-gesture trace: `tail_logs(module_contains="touch")` for the per-fire `touch dispatched: kind=hug rejected=false reason=ok` line. If `rejected=true reason=cooldown` / `daily_cap` / `axes_floor` the gate is doing its job — adjust `agent.touch_per_kind_overrides` or wait for axes to climb. **K32 reaction path**: 1) `add_user_reaction(message_id=N, kind="heart")` fakes a click against a real assistant message id (look one up via `get_status` → recent messages). The MCP tool returns the post-apply axes state — if `closeness` moves the apply path works end-to-end. 2) Grep `user_reaction axes:` (INFO level, fires whenever apply moves an axis); if the line shows `applied={...}` but the live axes don't change after a few clicks, the daily cap is exhausted (default `agent.user_reactions_daily_cap_per_axis=0.15`, ~5 hearts per axis per day) — call `add_user_reaction` for `surprise` (signal-only, no axis movement) to verify the inner-life cue still queues. 3) Confirm the multi-window sync: open the chat tab in two browser windows, click 🫂 in one — the other should update within a single WS roundtrip via `message_reaction_updated`. **Persona banner specifically**: gated on `agent.persona_touch_banner_enabled` AND `enabled` prop AND a live `avatarTouchAt` bump AND a backend assistant message id; if everything's set but the banner doesn't show, the source-level test ([`PersonaActionBanner.test.tsx`](../web/src/features/persona/PersonaActionBanner.test.tsx)) documents the exact subscription contract. Full reference + data-flow mermaid: [K31 + K32 in shipped.md](../docs/personality-backlog/shipped/patterns-k31-k60.md#k31--k32-soft-physicality-round-trip--virtual-touch--user-side-reactions). |

### d. Level cheat sheet

The level dial is a contract — every new log call must respect it.

- **ERROR** — only real failures (turn lost, engine crashed, non-2xx). Always shown.
- **WARNING** — degraded paths and retries (model warmup failed, voice switch failed, scheduler job raised but loop continued).
- **INFO** (default) — one structured line per turn (`turn done:`), lifecycle moments (boot, `ollama connected:`, `STT engine ready:`, `TTS engine ready:`, `TTS voice switched:`, `STT capture done:`, scheduler init/shutdown, compaction triggered, proactive spoken).
- **DEBUG** — the tweaking firehose (per-Ollama-call timings, scheduler per-drain summaries, prompt-assembler retrieval breakdown, STT capture lifecycle, TTS enqueue/play, RAG cache hits, full transcript previews).

Three rules about *what* a line says, all of which were learned the
expensive way (H27):

- **A line describing an outbound action is emitted only by the code path
  that performs it.** `privacy scrub REDACT` was logged at INFO by a
  function used both to build search queries *and* as a local yes/no
  detector, so 62 of 284 lines described searches that never happened.
  Detector callers now use `web_safe_probe()`, which is quiet on the happy
  path; `scrub_claim_for_search()` is the one that logs.
- **A line about malformed input carries a bounded preview of the input.**
  `llm-unparseable` without the payload is unactionable, and it hid a
  one-line schema mismatch for two months.
- **Absence needs a cause, not a count.** "found 0 items" and "the model
  returned nothing" must be different lines, or a permanently broken stage
  reads exactly like a quiet evening.

And keep dumps bounded: `dropped=[…]` printed one entry per occurrence, so
scrubbing a transcript produced a 196-element wall of repeated pronouns
that pushed the actual before/after out of view. De-duplicate and count.

Practical presets:

| Goal | How |
|---|---|
| Default | `level=INFO` in config. Turn-by-turn trace + lifecycle. |
| Investigate one module | Keep global `level=INFO`; bump just the relevant module via `set_log_level("app.core.session.prompt_assembler", "DEBUG")` (runtime) or `logging.module_levels` in `config/user.json` (persistent). |
| Production / quiet | `level=WARNING` or `level=ERROR` — a healthy session emits zero log lines. |
| Full firehose | `level=DEBUG` — only when actively tweaking; expect ~hundreds of lines per turn. |

### e. Practical workflow

1. **Before reproducing**: `set_log_level("app.core.<suspect>", "DEBUG")` on the module you suspect.
2. **Trigger the issue**: `send_message(skip_tts=true)` (or use the UI / voice).
3. **Read back**: `tail_logs(module_contains="<suspect>")` for the most recent ring entries, then `read_log_file(grep="turn=<id>")` to widen the lens to the whole turn.
4. **If you need a new field**: open the relevant module, add a structured log line at the right level (use `key=value` so a future grep stays cheap), document the new field in §b above, and restart the app.

### f. Time-travel (DT1 virtual clock)

Much of Aiko's behaviour is time-gated — memory decay and tier promotion, concept confidence decay, anniversaries, gap-return, and the cooldown on nearly every inner-life cue. Logs can only tell you a gate *didn't* open; to see what happens when it does, you used to have to wait. DT1 removes the waiting.

**Arming it.** Start the process with `AIKO_DEBUG_CLOCK=1`:

```bash
AIKO_DEBUG_CLOCK=1 python -m app.web          # bash
$env:AIKO_DEBUG_CLOCK=1; python -m app.web    # PowerShell
```

The gate is an environment variable rather than a setting so it costs a deliberate restart and can never persist itself into `config/user.json` in the on state. Without it the MCP tools are inert and say so.

**Point it at a copy of the database.** Rows written while the clock is shifted keep their virtual timestamps forever — a reset cannot rewrite them.

**Pick the right clock.** This is where people lose time:

| You want to test | Use | Why |
|---|---|---|
| Anniversaries, milestones, cue cooldowns, candidate TTL, promotion age, gap-return | `advance_clock(days=…)` | These read wall-clock `now`. |
| Concept (L3) confidence decay, memory decay, dormancy | `advance_engagement(days=…)` | These read *engaged* time — accumulated active conversation, ~1h per "day". A wall-clock advance does nothing to them. |

Decay is also catch-up-clamped per sweep (`concept_decay_max_catchup_days`, default 3), so simulating a long stretch means interleaving `advance_engagement(3)` with `force_concept_lifecycle` rather than one big jump. Sixty engaged days takes about a minute that way.

**Clean up with `reset_clock`.** It returns to real time and restores the engagement total from the undo anchor that `advance_engagement` stashes on first use. The wall-clock offset is in-memory only, so a restart also clears it.

**What does not move.** Everything on `time.monotonic()` / `time.time()`: turn latency, TTS/STT audio timing, the brain loop, HTTP and orchestrator deadlines, the scheduler's tick budget, worker perf metrics. Log and crash timestamps, task stall detection (`app/core/tasks/`), and outbound weather API dates also stay real. The dividing line is "`datetime` narrative time moves, monotonic runtime timing does not" — see [`app/core/infra/debug_clock.py`](../app/core/infra/debug_clock.py).

Tool reference: [`rules/mcp-server.md` → Virtual clock](mcp-server.md#virtual-clock-dt1).

### g. Frontend crashes

The UI reports its own crashes on a path that is **separate from the log
stream above** and, unlike the `[ui]` debug bridge, is not behind a
toggle: a crash you didn't expect is exactly the one nobody had debug
logging on for. The React error boundary plus the global `error` /
`unhandledrejection` listeners POST to `/api/logs/ui-crash`, which logs
one `ERROR [ui] crash` line and appends a structured entry to
`crashlog.txt`. Read them with **`get_ui_crashes()`**.

What each report carries, in the order worth reading it:

| Field | What it answers |
|---|---|
| `breadcrumbs` | *What led to this.* Last ~60 events, oldest first, stamped `+Nms` from page load. Sources: `console` (every `console.error`/`warn`, so React's own diagnostics are in here), `ws`, `api` (any non-2xx or network failure), `resource` (a failed image/script — a missing Live2D texture explains a lot of later "undefined" errors), plus every existing `debugLog` call site. Repeats inside a second collapse to a `count` so a reconnect loop can't flush the trail. |
| `context` | *What state it was in.* `build` id, `viewport`, `dpr`, `shell` (tauri/browser), `heapUsedMb` / `heapPct`, `visibility`, plus `connection`, `voiceMode`, `route`, `turnInProgress`, `sessionKey`. |
| `stack_mapped` | The stack **de-minified** against `web/dist/assets/*.map`. |
| `component_stack` | The React tree, for `source=render`. |

**On the stack.** A production bundle is minified, so the raw `stack` is
useless on its own; `build.sourcemap` in `web/vite.config.ts` exists to
make it recoverable, and [`app/core/infra/sourcemap.py`](../app/core/infra/sourcemap.py)
does the mapping **server-side** — deliberately, because the crashes
worth catching happen on a phone with no DevTools to attach. Correctness
comes free from Vite's content hashing: `index-D4x9k2.js` only ever
matches `index-D4x9k2.js.map`, so a stale `dist/` produces *no* mapping
rather than a wrong one. That is why both `stack` and `stack_mapped` are
recorded, and why a missing `stack_mapped` means "rebuild happened", not
"symbolication is broken".

**When adding anything that runs in the browser**, prefer
`addBreadcrumb(cat, msg, detail)` from
[`web/src/crashBreadcrumbs.ts`](../web/src/crashBreadcrumbs.ts) over a
bare `console.log` for anything you'd want to see in a post-mortem. It
is in-memory only and costs nothing until a crash reads it.

### h. Native crashes (the process just vanished)

`Windows fatal exception: access violation` in `crashlog.txt` is a
different animal from everything above: a memory fault inside a
C/C++/Rust extension, which kills the process outright. Read these with
**`get_native_crashes()`**.

**Do not debug the Python frame at the top of the `faulthandler` dump.**
A pure-Python statement cannot dereference a bad pointer, so that frame
is not the cause — it is wherever the already-corrupted heap happened to
be touched next, which in practice means the busiest allocation site in
the process. The first such crash we investigated pointed at
`topic_graph._live_to_topic_clusters_locked`, a function that had run
cleanly a hundred times that same hour and turned out to be entirely
innocent; the tell was that the "suspect" code path had succeeded twice
in the three minutes before the fault.

What to read instead, in order:

| Field | What it answers |
|---|---|
| `module` | *Which DLL held the bad pointer.* Resolved from the faulting address via `GetModuleHandleEx`. Usually the whole answer. |
| `thread_name` | *Whose thread faulted*, from `GetThreadDescription`. Read this second — it decides whether the fault is even yours to fix, and it costs nothing. See below. |
| `exception` / `address` | The Windows exception code and faulting pointer. An address like `0x0000000000000010` means a null-ish struct deref; a wild address means corruption. |
| `duplicate_openmp` / `openmp_runtimes` | Whether the process was in a **supported configuration at all** — see below. Check this before believing any other theory. |
| `dump` | The minidump **parsed in place**, no debugger and no symbols needed — see below. |
| `minidump` | Path to the `.dmp` under `data/`, for a real stack walk in WinDbg / Visual Studio if the parsed summary isn't enough. |
| `minidump_degraded` | Present when the dump was written *without* its exception record (dbghelp refuses the exception-pointers struct from inside a filter with `ERROR_NOACCESS`). Such a dump cannot self-report where it faulted, so a debugger won't jump to the fault and the reader has to be handed the `address` / `thread_id` above. |

**The thread name is the cheapest decisive fact.** A fault on
`tokio-rt-worker` happened inside a dependency's own async runtime, where
our locking and our calling pattern are not the lever; the same fault on
`MessageIndexer` or `rag-search_1` would be ours. The first LanceDB fault
we investigated looked at first like a concurrency bug in `RagStore` —
three `rag-search` threads do genuinely read the same Lance tables at once
by design (P19) — and the thread name is what ruled that out in one line:
it faulted on `tokio-rt-worker`, so no amount of locking on the Python
entry points could have prevented it. A single `search()` fans out onto
that runtime regardless of how many callers our own lock lets through.

**Reading the dump without a debugger.** `cdb`/WinDbg is not installed on
the dev box and nobody ships symbols for a 147 MB Rust extension, so a
`.dmp` used to be evidence nobody could open.
[`app/core/infra/minidump.py`](../app/core/infra/minidump.py) parses the
streams that matter directly and `get_native_crashes()` attaches the result
as `dump`:

- `fault_module` — the address resolved to `name.dll+0xOFFSET`, from the
  dump's own module list (so it works even for an unloaded module).
- `fault_thread_name` and `thread_census` — names for every thread, counted.
  Worth reading against `processors`: 102 threads named `tokio-rt-worker`
  on a 32-core box is how you find out a dependency sizes its own pool
  well above core count, which is a fact about the library rather than a
  leak of ours (one `RagStore`, one `lancedb.connect`).
- `fault_stack_modules` — modules found on the faulting thread's saved
  stack. This is a **scan for values that land inside a loaded module, not
  a stack walk**: it picks up dead frames and stale pointers, so read it as
  "what this thread had touched" and never as a call order. It is enough to
  tell "Python called into this" apart from "this ran entirely inside the
  native runtime", which is the distinction that matters.
- `fault_stack_handler_frames` — the part of that stack contributed by our
  own crash handler (`dbgcore`, `dbghelp`, `libffi`, `_ctypes`), because the
  unhandled-exception filter is a ctypes callback that runs *on the faulting
  thread*. Ignore it; it is reported only so nobody mistakes it for
  evidence and goes auditing the crash handler.

**Multiple OpenMP runtimes is the first thing to rule out.** Two copies
of one runtime, or two different ones, in a single process is documented
undefined behaviour: each keeps its own global state and thread pool. The
result is a random native fault after hours of uptime — which looks
exactly like failing hardware, and which CPU/RAM stress tests will never
reproduce, so it survives a clean bill of health from memtest or OCCT.
`log_native_runtime_inventory()` logs the tally once at boot through the
`app.native_runtimes` logger, at WARNING when the combination is
unsupported, so grep `native runtimes:` to see what a given run was
carrying.

**Know the expected baseline, or the field is unreadable.** The main
process should map exactly one OpenMP runtime,
`torch\lib\libiomp5md.dll`, and the venv should contain no `vcomp140.dll`
at all. A duplicate in the main process therefore means something
regressed, and the two candidates are worth knowing because both have
happened: **an environment behind `requirements.lock`** (`realtimestt`
0.3.x imports `openwakeword` and `faster_whisper` eagerly, which adds
CTranslate2's copy of `libiomp5md.dll` plus sklearn's `vcomp140.dll` — the
1.x line loads both lazily instead), or **an eager import of the STT
service's dependencies** defeating the `_recorder_class()` deferral. Start
with `pip check` and a diff against the lock; that is seconds of work and
it was the actual answer last time. Note that the *transcription child*
legitimately holds both copies (`faster_whisper` pulls torch via
`transformers`) — on Windows that is a separate `mp.Process`, but
`start_recorder_worker` uses a thread on Linux, so the Docker image puts
that duplicate in the main process. The classification lives in
[`app/core/infra/native_runtimes.py`](../app/core/infra/native_runtimes.py)
— note that it matches curated DLL names rather than substrings, because
`_decomp_lu_cython.pyd` and `_compute.pyd` contain "omp" and make a naive
scan report a pile of runtimes that don't exist.

Those DLLs load on the first *real* STT use, not at import: the
`RealtimeSTT` import in
[`app/stt/realtime_stt_service.py`](../app/stt/realtime_stt_service.py)
is deferred behind `_recorder_class()`, and availability is answered with
`find_spec` so the voice gates don't trigger it. A text-only session
therefore loads no OpenMP at all — which also means the inventory line
differs between a voice run and a text run, and that is expected.

Two caveats when reading the file itself. It is **append-mode**, so dumps
accumulate across runs and the newest is at the bottom; a single header
means a single native crash, no matter how many times the app has died.
And a fatal dump with no matching `native_crash` record predates this
handler — restart to arm it.
